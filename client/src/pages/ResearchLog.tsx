/**
 * Research Ledger design: chronological research entries preserve uncertainty in an editorial ledger,
 * with explicit evidence stamps, archival chapter clusters, and a durable right-side provenance spine.
 */
import { ArrowUpRight, CalendarDays, Database, FileText, FileWarning, GitCommitHorizontal, Lightbulb, ShieldCheck, Workflow } from "lucide-react";
import { StatusStamp } from "@/components/StatusStamp";

const entries = [
  { date: "24 AUG 2026", title: "Pure FedProx manifest eligibility is quality-gated without data loading or training", status: "VALIDATED" as const, icon: ShieldCheck, body: "Agent revision 950491e implements the one-use pure synthetic FedProx manifest validator. It accepts an exact frozen experiment-intent class, privately canonicalizes allowlisted fixture, opaque seed, hyperparameter, local-epoch, shape, and retained-control classes, emits frozen scalar receipt/readout, and suppresses replay. Six focused checks passed; the full local chain passed 203 TypeScript and 4 Python tests; remote Quality Gates run 32754612654 completed successfully. This is CI-backed source quality only. No data was loaded, no model or weight created, no trainer or Agent ran, and no update submission or aggregation occurred." },
  { date: "24 AUG 2026", title: "FedProx execution begins with a pure manifest validator, not a trainer", status: "PROVISIONAL" as const, icon: FileText, body: "The first executable FedProx slice is scoped to one frozen synthetic manifest intent. It recognizes only versioned classes for synthetic fixture, opaque deterministic seed, proximal and optimizer hyperparameters, local epochs, parameter shape, and retained no-trainer/no-submission/aggregation-disabled controls. It consumes once, projects frozen scalar eligibility or closure, and suppresses replay. It imports no Python runner, trainer, data/model/weight path, environment, filesystem, network, Agent runtime, submission, or aggregation module. The resulting evidence can establish intent eligibility only; it cannot establish training, metrics, model quality, submission, or aggregation." },
  { date: "24 AUG 2026", title: "FedProx scientific evidence is separated from delivery and deployment through a synthetic-only design", status: "PROVISIONAL" as const, icon: FileText, body: "The next thesis boundary is now an immutable synthetic-only FedProx experiment contract. It binds an opaque synthetic fixture class, deterministic seed class, versioned algorithm contract, allowlisted hyperparameter classes, scalar-only metrics, finite local-epoch checks, arithmetic and replay closure, cleanup, and a narrow claim vocabulary. Current Python checks establish toy arithmetic and shape rejection only; they do not train a model, process a dataset, report accuracy, or represent a hospital workload. Any later synthetic training, update submission, and aggregation remain independent gates." },
  { date: "24 AUG 2026", title: "One-shot synthetic proof is designed as a finite private lifecycle, not an open-ended runtime exercise", status: "PROVISIONAL" as const, icon: FileText, body: "The direct path now defines a future proof only after all five independent evidence packets are current. It permits one opaque synthetic-operation class, one private non-public process, a fresh aggregation-disabled check, one terminal observation, and mandatory cleanup. The finite lifecycle closes on expiry, revocation, mismatch, duplicate admission, pre-route cancellation, interruption, failure, residue, or cleanup uncertainty with no retry, packet refresh, target substitution, fallback identity, public listener, trainer, submission, or aggregation. The record is aggregate-safe only and cannot prove clinical integration, model quality, FedProx training, update submission, or aggregation." },
  { date: "24 AUG 2026", title: "Protected-composition preflight is reduced to five independent evidence packets", status: "PROVISIONAL" as const, icon: FileText, body: "The next direct prerequisite is now a redacted evidence contract, not another code seam. A one-shot synthetic proof remains closed until source-quality, protected package/identity authorization, target-bound release, protected composition/configuration, and fresh Core-control packets are independently valid and current. The finite preflight closes on malformed, mismatched, broadened, stale, revoked, duplicate, or unsafe input without target contact, process start, credential retry, fallback identity, or public listener. The review authorizes no runtime action and preserves the separate later gates for Agent proof, FedProx training, update submission, and aggregation." },
  { date: "24 AUG 2026", title: "Direct durable-adapter hardening is quality-gated; runtime remains explicitly blocked", status: "VALIDATED" as const, icon: ShieldCheck, body: "Agent revision 6d1235f hardens the temporary-test-root claim-terminal adapter: only canonical record names are allowed; unexpected or temporary material closes; complete canonical bytes are written before flush and rename; symbolic/non-private roots, record symlinks, malformed/oversized/truncated records, interrupted state, duplicate transitions, and replay close. Five focused checks passed; the full local chain passed 197 TypeScript and 4 Python tests; remote Quality Gates run 32733992659 completed successfully. The published prerequisite checklist confirms that package/identity, target-bound release, protected composition, non-public invocation, and fresh Core-control evidence remain separate blockers. No runtime action, proof, training, submission, or aggregation occurred." },
  { date: "24 AUG 2026", title: "Direct thesis path now targets the protected local durable-adapter boundary", status: "PROVISIONAL" as const, icon: FileText, body: "The delivery path has moved from additional isolated scalar seams to the concrete durable boundary needed before any bounded synthetic Agent proof can be considered. The design names one future protected composition owner, opaque configuration and root-capability flow, strict private-root acceptance, fixed claim-terminal records, full-write/flush/rename posture, residue and cleanup closure, scalar-only observability, and compatibility fixtures. The next implementation may improve temporary-test-root mechanics only. It may not resolve configuration, construct protected composition, wire a runtime, access a target/package/credential, start an Agent, stage, prove, train, submit, or aggregate." },
  { date: "24 AUG 2026", title: "Scalar adapter-construction eligibility is quality-gated without constructing an adapter or port", status: "VALIDATED" as const, icon: ShieldCheck, body: "Agent revision 139db42 implements the one-use scalar eligibility evaluator. It accepts one frozen symbolic envelope, privately canonicalizes only all-allowlisted intent, and returns frozen scalar receipt/readout. Eight focused checks passed; the full local chain passed 196 TypeScript and 4 Python tests; remote Quality Gates run 32727048481 completed successfully. Coverage includes eligible, unresolved/invalid configuration, construction-state, root-binding, runtime-dependency, malformed, replay, independent-instance, freezing/redaction, nonenumerability, and import-isolation closure. This is CI-backed source quality only. No adapter/port construction, configuration/root resolution, filesystem/environment action, application/runtime wiring, staging, proof, training, submission, or aggregation action occurred." },
  { date: "24 AUG 2026", title: "Adapter-construction eligibility is bounded as scalar intent only", status: "PROVISIONAL" as const, icon: FileText, body: "The next source-only evaluator consumes one exact frozen symbolic envelope: configuration intent, construction state, root binding, and dependency surface. Only reference-validated, adapter-unconstructed, not-injected, scalar-only intent is eligible for a future separate review; every unresolved, denied, root-requested, runtime-dependent, malformed, or replayed case closes with an allowlisted scalar receipt. It privately canonicalizes intent but returns no envelope, adapter, port, root, path, record, configuration, target, credential, function, or runtime object. No adapter/port construction, configuration or root resolution, filesystem/environment action, application/runtime wiring, staging, proof, training, submission, or aggregation action is authorized." },
  { date: "24 AUG 2026", title: "Scalar configuration-reference validator is quality-gated without resolution or root access", status: "VALIDATED" as const, icon: ShieldCheck, body: "Agent revision 543f172 implements the pure one-use validator for a frozen symbolic private-root reference intent. It privately canonicalizes only allowlisted category, binding, and state, then emits frozen scalar receipts and aggregate readouts. Six focused checks passed; the full local chain passed 188 TypeScript and 4 Python tests; remote Quality Gates run 32726445658 completed successfully. Coverage includes valid input, malformed and unsupported closure, replay after valid or invalid input, independent instances, freezing/redaction, marker nonenumerability, and import isolation. This is CI-backed source quality only. No configuration resolution, environment/secret/root/path/filesystem action, application/runtime wiring, staging, proof, training, submission, or aggregation action occurred." },
  { date: "24 AUG 2026", title: "Scalar configuration-reference validation is bounded before any configuration or root resolution", status: "PROVISIONAL" as const, icon: FileText, body: "The next source-only slice accepts only one exact frozen symbolic reference with schema version, private-root-reference category, protected-composition-only binding, and unresolved-only state. It neither resolves nor dereferences the object. Valid input yields a frozen scalar validation receipt; mutable, inherited, malformed, unknown, missing, unsupported, and replay cases close terminally. The private canonical intent must not enumerate or serialize, while receipts and snapshots reveal aggregate counts only. No environment, configuration, secret, root, path, filesystem, target, credential, application/runtime, staging, proof, training, submission, or aggregation behavior is authorized." },
  { date: "24 AUG 2026", title: "Protected target-root capability is bounded as a separate design-only gate", status: "PROVISIONAL" as const, icon: FileText, body: "The next review authorizes no root injection. It defines a future protected composition module as the sole resolver of one opaque deployment configuration reference and the sole constructor handoff point for one private local-state adapter. The root, reference, path, records, bytes, ownership/mode facts, errors, configuration value, target, credential, and runtime object must never cross the scalar port boundary. Missing, malformed, non-private, shared, symlinked, unavailable, cleanup-uncertain, or replayed states close before coordinator admission, with only allowlisted aggregate codes. A future source-only configuration-reference validator fake is the next possible slice. No filesystem/environment action, application/runtime wiring, target interaction, staging, proof, training, submission, or aggregation action is authorized." },
  { date: "24 AUG 2026", title: "Scripted-port test composition is quality-gated without temporary-root or runtime wiring", status: "VALIDATED" as const, icon: ShieldCheck, body: "Agent revision 923b8eb implements the pure scripted-port composition. It accepts one frozen exact scalar script, privately constructs one in-memory scalar port, injects it into one fake coordinator, consumes once, and returns only a frozen scalar assertion. Eight focused checks passed; the full local chain passed 182 TypeScript and 4 Python tests; remote Quality Gates run 32725658219 completed successfully. Coverage includes normal and hydration/claim/route/terminal/disposal/replay closures, malformed input before construction, isolation, redaction, and import separation. This is CI-backed source quality only. No temporary-root factory call, application wiring, coordinator runtime, target root, filesystem/environment/network action, staging, proof, training, submission, or aggregation action occurred." },
  { date: "24 AUG 2026", title: "Scripted-port test composition is specified as the first executable ownership slice", status: "PROVISIONAL" as const, icon: FileText, body: "The next source-only slice accepts one strict versioned scalar script, internally constructs one fresh in-memory scalar port, injects it into one fake coordinator, admits once, and returns only a frozen scalar assertion. The script contains hydration, claim, terminal, and no-artifact-route outcomes only; invalid input closes before construction and any second call is composition replay. Normal, hydration, claim, route, terminal, disposal, replay, redaction, freezing, independent-composition, and import-isolation fixtures are required. It must not call the temporary-root factory or expose any root, correlation, record, path, text, configuration, target, credential, identity, transport, or runtime capability. No application wiring, target root, filesystem/environment/network action, staging, proof, training, submission, or aggregation action is authorized." },
  { date: "24 AUG 2026", title: "Application-composition ownership is bounded as a design-only test seam", status: "PROVISIONAL" as const, icon: FileText, body: "The next review designates a future test-composition module as the sole owner of one temporary-root factory input, private correlation, one scalar port, and one new fake coordinator. It may inject only the five-operation scalar interface, admit once, retain frozen aggregate-only assertions, dispose once if still open, and discard the pair. Construction failure, any denial, disposal, replay, or unexpected fixture exception closes with no replacement port or retry. The interface takes and returns no root, correlation, record, path, text, configuration, target, credential, transport, identity, stateful object, or capability. No application wiring, runtime change, target root, filesystem/environment/network action, parser, renderer, configuration, package/credential, staging, proof, training, submission, or aggregation action is authorized." },
  { date: "24 AUG 2026", title: "Fake coordinator compatibility fixture is quality-gated with no runtime wiring", status: "VALIDATED" as const, icon: ShieldCheck, body: "Agent revision e907f20 implements the documented test-only fake coordinator against an injected five-operation scalar port. Eight focused deterministic checks passed; the full local chain passed 174 TypeScript and 4 Python tests; remote Quality Gates run 32724807579 completed successfully. The fixture covers normal no-artifact terminal closure, terminal/orphan/invalid hydration, claim and terminal denials, route closure, disposal, replay, isolation, freezing, redaction, and import separation. This is CI-backed source quality only. No application composition, coordinator runtime, target root, parser, renderer, configuration, target, package/credential, staging, proof, training, submission, or aggregation action occurred." },
  { date: "24 AUG 2026", title: "Fake coordinator compatibility fixture is specified as an isolated scalar-only test boundary", status: "PROVISIONAL" as const, icon: FileText, body: "The next increment introduces a new test-only fake coordinator, not the existing admission coordinator. It receives only five structural scalar durability operations and can admit one synthetic no-artifact flow: hydrate, claim, deterministic no-artifact route, terminal. Terminal/orphan/invalid hydration and claim denial must close before route; route/terminal denial, disposal, and replay close without retry or port replacement. Receipts and counts are frozen and must reveal no root, correlation, record, port state, text, bytes, configuration, target, credential, or runtime object. No application wiring, target root, parser, renderer, configuration, target, package/credential, staging, proof, training, submission, or aggregation action is authorized." },
  { date: "24 AUG 2026", title: "Coordinator compatibility is bounded as a test-only scalar interface", status: "PROVISIONAL" as const, icon: FileText, body: "The new design gives a future source-only coordinator fake only five scalar durability operations: hydrate, claim, terminal, dispose, and snapshot. A fixture composition root privately constructs one temporary-root port and injects it into one fake coordinator; no root, correlation, record, path, factory input, or port state may cross the interface. Normal flow is hydrate-empty, claim, deterministic no-artifact route, and terminal; terminal/orphan hydration, claim denial, route/terminal denial, disposal, and replay close deterministically. No port is wired, no coordinator runtime changes, and no target-root, parser, renderer, configuration, target, package/credential, staging, proof, training, submission, or aggregation action is authorized." },
  { date: "24 AUG 2026", title: "Temporary-root adapter-port factory passes Agent quality gates", status: "VALIDATED" as const, icon: ShieldCheck, body: "Agent release `059c9c8` implements the local temporary-root port factory, private parallel-store/root/correlation closure, frozen readout, one-use lifecycle, fixtures, and import guard. The port exposes only hydrate, claim, terminal, dispose, and snapshot. Terminal/orphan hydration, claim or terminal denial, and disposal close; later calls are replay-suppressed. Fixtures prove canonical lifecycle, temporary-residue claim denial, independent ports, disposal, and redaction. Local CI passed 166 TypeScript and four Python tests; Hospital Node Quality Gates `32723808639` succeeded. No target root, application wiring, coordinator, parser, renderer, configuration, target, package/credential, staging, proof, training, submission, or aggregation action occurred." },
  { date: "24 AUG 2026", title: "Temporary-root adapter-port factory contract is specified before implementation", status: "PROVISIONAL" as const, icon: FileText, body: "The next local-only increment constructs an operation-scoped scalar port from a test-created temporary root and private synthetic correlation. The returned port exposes only hydrate, append claim, append terminal, dispose, and aggregate snapshot; it accepts and projects neither root nor correlation. Its lifecycle is one-way: hydrate before claim before terminal, while every terminal, denial, invalid, orphan, or dispose state closes in memory and suppresses reuse. Fixtures must prove independent ports, scalar closure order, disposal/replay, and redaction. No application wiring, target root, coordinator, parser, renderer, configuration, target, package/credential, staging, proof, training, submission, or aggregation action is authorized." },
  { date: "24 AUG 2026", title: "Local durable adapter-port integration is bounded by an operation-scoped scalar design", status: "PROVISIONAL" as const, icon: FileText, body: "The new review requires an operation-scoped port constructed only in a local composition root. Its private closure owns the parallel adapter, constructor-injected root, and opaque correlation; the coordinator sees only hydrate, append-claim, and append-terminal scalar results. No root, correlation, record, text, bytes, error, filesystem fact, configuration, target, credential, or runtime object can cross the port. Temporary-root compatibility fixtures must verify route order, terminal/orphan/claim/terminal failure closure, replay disposal, and redaction. No port is wired, no target root injected, and no parser, renderer, configuration, target, package/credential, staging, proof, training, submission, or aggregation action is authorized." },
  { date: "24 AUG 2026", title: "Parallel local durable adapter passes Agent quality gates with temporary-root fixtures", status: "VALIDATED" as const, icon: ShieldCheck, body: "Agent release `ce171d8` implements the parallel local claim-terminal adapter, constructor-injected temporary-root fixtures, canonical scalar claim/terminal validation, atomic temporary/flush/promote lifecycle, aggregate readout, and filesystem isolation. It is separate from the authorization-diagnostic store. Fixtures prove empty-to-terminal closure, duplicate/mismatch rejection, malformed/noncanonical and temporary-residue closure, symlink/mode root denial, and redaction. Local CI passed 162 TypeScript and four Python tests; Hospital Node Quality Gates `32723031344` succeeded. No target root, composition wiring, coordinator, parser, renderer, configuration, target, package/credential, staging, proof, training, submission, or aggregation action occurred." },
  { date: "24 AUG 2026", title: "Parallel local durable claim-terminal adapter is specified for temporary-root fixtures only", status: "PROVISIONAL" as const, icon: FileText, body: "The next Agent increment implements a parallel local adapter only against test-created temporary roots. Its constructor receives a test-only private root; it persists canonical scalar claim then terminal records in a non-overlapping slot, projects only scalar closure/readout classes, and keeps correlation private. Strict root/mode/ownership/symlink/canonical JSON/sequence/atomic-promotion/cleanup checks close on every invalidity. Valid terminal or orphan claim hydration suppresses further work. It has no application composition, target root, parser, renderer, configuration, package/credential, target, staging, proof, training, submission, or aggregation capability." },
  { date: "24 AUG 2026", title: "Concrete durable-adapter composition is bounded by a parallel-store design review", status: "PROVISIONAL" as const, icon: FileText, body: "The existing authorization-diagnostic durable store cannot be reused for coordinator claim/terminal records: it accepts one fixed blocked-state diagnostic record and no claim, correlation, or two-event lifecycle. The new review requires a parallel local-only adapter, constructor-only private-root capability, canonical scalar claim then terminal sequence, atomic internal promotion, strict hydrate, and aggregate-only readout. Temporary residue, record integrity, mode/ownership/symlink, permission, and transition failures close terminally. No adapter is constructed, root accessed, record written, parser/renderer/configuration action, target, package/credential, staging, proof, training, submission, or aggregation action is authorized." },
  { date: "24 AUG 2026", title: "Fake durable-store composition adapter passes Agent quality gates", status: "VALIDATED" as const, icon: ShieldCheck, body: "Agent release `34d2d5d` implements the source-only deterministic composition adapter, private scalar record/route ports, in-memory operation closure set, frozen readout, fakes, and import guard. It routes only after empty hydrate and claim-recorded; the terminal category then has one terminal append attempt. Malformed input, terminal/orphan/hydrate closure, claim denial, terminal append failure, and replay close terminally. Local CI passed 158 TypeScript and four Python tests; Hospital Node Quality Gates `32721983548` succeeded. No concrete durable store, root, record/file, filesystem, parser, renderer, configuration, target, package/credential, staging, proof, training, submission, or aggregation action occurred." },
  { date: "24 AUG 2026", title: "Fake-only durable-store composition adapter is specified before implementation", status: "PROVISIONAL" as const, icon: FileText, body: "The next Agent increment models coordinator durability using only private deterministic scalar ports: hydrate, append claim, append terminal, and one scalar route. Empty state must claim before route; terminal/orphan/hydrate denial cases call no route. Terminal append failure closes the in-memory operation without retry. Fakes return classes and counts only, never roots, records, text, bytes, configuration, parser data, artifacts, targets, credentials, or provider facts. No durable store is wired or root/record created; no parser, renderer, configuration, target, package/credential, staging, proof, training, submission, or aggregation action is authorized." },
  { date: "24 AUG 2026", title: "Durable-store composition is bounded as a private scalar closure design", status: "PROVISIONAL" as const, icon: FileText, body: "The next review defines an injected coordinator terminal-record port between the admission coordinator and the existing local durable adapter. Only the local-state composition root may own the private-root capability. The port hydrates an opaque correlation to scalar closure, appends one claim, and appends one terminal category; it cannot enumerate, query, delete, expose records, paths, text, bytes, identity values, or root state. Hydrate precedes claim, claim precedes one coordinator route, and terminal append follows it. Any invalidity or persistence failure closes terminally. No store is wired, root requested, record created, parser/renderer/configuration action, target, package/credential, staging, proof, training, submission, or aggregation action is authorized." },
  { date: "24 AUG 2026", title: "Coordinator durability is bounded as a claim-and-terminal scalar ledger design", status: "PROVISIONAL" as const, icon: FileText, body: "The next durability review defines a private append-only scalar ledger: an opaque operation correlation, claimed state before coordinator invocation, and one terminal class after it. Hydration validates canonical monotonic records; a terminal record suppresses re-entry, while an orphan claim closes as interrupted after restart. Corruption, permission, temporary, atomic, symlink, duplicate, and non-monotonic failures close before routing. Public readout exposes aggregate categories only. This review neither wires storage nor creates a private root or record. No parser, renderer, configuration text/file, target, package/credential, staging, proof, training, submission, or aggregation action is authorized." },
  { date: "24 AUG 2026", title: "Synthetic admission coordinator passes Agent quality gates", status: "VALIDATED" as const, icon: ShieldCheck, body: "Agent release `26b471b` implements the source-only coordinator, private validator/policy ports, operation replay set, frozen readout, integration fixtures, and import guard. It validates matched operation/shape/render identities, calls validator first, reaches policy only after inert-shape, and yields not-rendered-after-discard only after symbolic discard. Malformed/cross-identity inputs invoke neither component; validator denial reaches no policy/workspace; policy and cleanup closures remain no-artifact and promotion-disabled. Local CI passed 154 TypeScript and four Python tests; Hospital Node Quality Gates `32711294107` succeeded. No parser, renderer, configuration text/file, filesystem, target, package/credential, staging, proof, training, submission, or aggregation action occurred." },
  { date: "24 AUG 2026", title: "Synthetic admission coordinator contract is specified before validator-policy composition", status: "PROVISIONAL" as const, icon: FileText, body: "The next Agent increment composes the existing scalar validator and renderer-policy fakes. A versioned envelope must prove an exact synthetic operation/shape/render identity relation before the operation is consumed. The coordinator invokes validator first and policy only after an inert-shape receipt. Malformed or cross-identity envelopes call neither component; validator denial reaches no policy/workspace. All outputs are frozen scalar receipts and terminal replay closure; symbolic policy/workspace closure remains no-artifact and promotion-disabled. No parser, renderer, configuration text/file, filesystem, target, package/credential, staging, proof, training, submission, or aggregation action is authorized." },
  { date: "24 AUG 2026", title: "Validator-to-renderer-policy injection is bounded as a scalar-only design review", status: "PROVISIONAL" as const, icon: FileText, body: "The next review defines a future coordinator that owns synthetic operation identity, calls the scalar validator first, and invokes renderer policy only after an inert-shape receipt. The validator retains its private canonical object; the coordinator and policy see scalar classes only. Malformed or cross-identity envelopes call neither component. Validator denial reaches no policy/workspace, while policy/workspace closures remain no-artifact and promotion-disabled. Fixtures require one-way identity consumption, deny-before-workspace ordering, scalar readouts, and redaction. No coordinator implementation, parser, renderer, configuration text/file, filesystem, target, package/credential, staging, proof, training, submission, or aggregation action is authorized." },
  { date: "24 AUG 2026", title: "Synthetic scalar abstract-shape validator passes Agent quality gates", status: "VALIDATED" as const, icon: ShieldCheck, body: "Agent release `1408470` implements the source-only scalar validator, private canonical inert marker, immutable request normalization, frozen readout, replay suppression, fixtures, and import guard. Exactly one canonical synthetic object yields inert-shape-validated; the canonical marker and identity state remain nonenumerable and absent from serialization. Malformed, unknown, missing, inherited, mutable, duplicate, controls, binding/image, command/promotion, and replay cases close terminally. Local CI passed 149 TypeScript and four Python tests; Hospital Node Quality Gates `32710333192` succeeded. No text parser, renderer, configuration text/file, filesystem, target, package/credential, staging, proof, training, submission, or aggregation action occurred." },
  { date: "24 AUG 2026", title: "Synthetic scalar abstract-shape validator contract is specified before implementation", status: "PROVISIONAL" as const, icon: FileText, body: "The next Agent increment implements a pure synthetic scalar validator. It accepts only one exact versioned disabled/no-ingress/no-restart/no-host-network/reserved-binding/fresh-image/no-command/promotion-disabled shape, then builds a new frozen private canonical result and emits scalar outcomes. Arrays, non-objects, mutable/unknown/missing/invalid input, every control/binding/image/command/promotion denial, and replay must close terminally. It invokes no workspace or renderer policy. No text parser, renderer, configuration text/file, filesystem, target, package/credential, staging, proof, training, submission, or aggregation action is authorized." },
  { date: "24 AUG 2026", title: "Concrete abstract-shape validator review defines immutable scalar normalization without text parsing", status: "PROVISIONAL" as const, icon: FileText, body: "The next validator design consumes only an exact versioned synthetic scalar shape. It accepts one canonical disabled/no-ingress/no-restart/no-host-network/reserved-binding/fresh-image/no-command/promotion-disabled state and maps all other allowlisted classes to terminal denials. Validation recreates a frozen canonical object in private fields, rejects mutable/unknown/missing/replay input, and emits only inert-shape or scalar denial. A separate future parser must project duplicate syntax as a scalar denial; this boundary parses no text. Fixtures remain synthetic and source-only. No renderer, configuration text/file, filesystem, target, package/credential, staging, proof, training, submission, or aggregation action is authorized." },
  { date: "24 AUG 2026", title: "Injected fixed-shape parser and workspace policy passes Agent quality gates", status: "VALIDATED" as const, icon: ShieldCheck, body: "Agent release `05a87a3` implements the source-only injected renderer policy, private fixed-shape parser fake, private blueprint/workspace ports, frozen readout, and import guard. Exactly one inert abstract shape can progress through symbolic temporary, validation, and discard to not-rendered-after-discard; promotion has no callable operation and promotion requests deny before all fakes. Malformed, workspace, fallback, blueprint, every parser class, temporary, validation, cleanup, and replay cases close terminally. Local CI passed 144 TypeScript and four Python tests; Hospital Node Quality Gates `32709516280` succeeded. No renderer execution, text/file, filesystem, target, package/credential, staging, proof, training, submission, or aggregation action occurred." },
  { date: "24 AUG 2026", title: "Injected fixed-shape parser and workspace policy is specified before implementation", status: "PROVISIONAL" as const, icon: FileText, body: "The next Agent increment composes existing deterministic blueprint/workspace fakes with an injected parser fake. It accepts only an unused synthetic identifier, scalar disabled-not-rendered intent, private non-path blueprint sentinel, workspace-ready class, and promotion-disabled-by-policy. The parser can emit fixed inert or allowlisted denial shape classes; only inert may reach symbolic temporary, validation, and discard. Promotion has no callable path. Malformed, fallback, availability, parser, workspace, promotion, and replay cases must close terminally with scalar-only results. No renderer, configuration text/file, filesystem, target, package/credential, staging, proof, training, submission, or aggregation action is authorized." },
  { date: "24 AUG 2026", title: "Concrete static renderer is bounded to a source-only fixed-shape implementation plan", status: "PROVISIONAL" as const, icon: FileText, body: "The next implementation plan permits only injected validator/parser/workspace policy contracts against deterministic fakes. A private blueprint handle can yield an abstract fixed inert-shape class, while the workspace models temporary validation and discard; promotion has no callable operation and returns only promotion-disabled-by-policy. Fixed parsing rejects unknown, mutable, active, exposed, restart, host-network, fallback, stale, command-bearing, and promotion-enabled shape classes. Receipts and counters remain scalar-only. No renderer execution, text/file, filesystem, target configuration, package/credential access, staging, proof, training, submission, or aggregation action is authorized." },
  { date: "24 AUG 2026", title: "Deterministic blueprint and private-workspace fakes pass Agent quality gates", status: "VALIDATED" as const, icon: ShieldCheck, body: "Agent release `f829bfd` implements a pure fake-only renderer orchestrator, private nonenumerable synthetic blueprint reference, scalar blueprint fake, symbolic workspace fake, frozen aggregate readout, and import guard. One fixed candidate can progress only through fake availability, temporary, inert validation, and discard to not-rendered-after-discard. Malformed, unknown, missing, non-disabled, workspace, fallback, blueprint, temporary, validation, cleanup, and replay cases close terminally. Local CI passed 139 TypeScript and four Python tests; Hospital Node Quality Gates `32708496990` succeeded. No renderer, configuration text/file, filesystem, environment, image/root binding, package/credential/target, staging, proof, training, submission, or aggregation action occurred." },
  { date: "24 AUG 2026", title: "Fake-only blueprint and private-workspace contract is specified before implementation", status: "PROVISIONAL" as const, icon: FileText, body: "The next Agent increment is confined to in-memory deterministic fakes. A request contains only an unused synthetic identifier, scalar disabled-not-rendered intent, a fixed non-path synthetic blueprint sentinel, and a scalar workspace state. The blueprint fake returns only available/unavailable/invalid/fallback-denied classes; the workspace fake advances only through symbolic temporary/inert/discard/denial states. Neither fake returns content, a path, a byte count, an artifact, or promotion. Unknown, missing, duplicate, fallback, unavailable, invalid, temporary, validation, cleanup, and replay cases close terminally. No renderer, configuration text or file, filesystem, target, package, credential, staging, proof, training, submission, or aggregation action is authorized." },
  { date: "24 AUG 2026", title: "Concrete static-renderer review preserves internal-only blueprints and no-artifact posture", status: "PROVISIONAL" as const, icon: FileText, body: "The next design review separates a future fixed concrete renderer from policy, package access, image/root binding, identity, deployment, and runtime. A target-owned opaque blueprint may be used only after a scalar disabled-not-rendered intent. Its internal temporary text must be bounded, validated for no-port/no-ingress/no-restart/no-host-network and no training/submission/aggregation controls, then discarded; atomic promotion is forbidden until a later deployment gate. Receipts expose only allowlisted scalar classes and aggregate counts. The design introduces no blueprint, renderer, file, target configuration, package/credential access, staging, proof, training, submission, or aggregation action." },
  { date: "24 AUG 2026", title: "Static composition render-intent policy passes Agent quality gates", status: "VALIDATED" as const, icon: ShieldCheck, body: "Agent release `ad2e2ed` implements the pure versioned static render-intent validator, terminal decision policy, private deterministic sink, frozen readouts, and import guard. Only one eligible request can record a scalar disabled-not-rendered result. Malformed, unknown, missing, configuration/image/injection/binding, fallback, activation/exposure/restart/network, sink-failure, and replay cases close terminally. Local CI passed 134 TypeScript and four Python tests; Hospital Node Quality Gates `32707838712` succeeded. No configuration artifact, renderer, process, filesystem, environment, root/image binding, package/credential/target, staging, proof, training, submission, or aggregation action occurred." },
  { date: "24 AUG 2026", title: "Source-only static composition render-intent policy is specified before implementation", status: "PROVISIONAL" as const, icon: FileText, body: "The next Agent increment permits only exact versioned scalar labels: eligible-ready-not-activated configuration, fresh image eligibility, prepared synthetic injection, reserved opaque binding, disabled activation, no public ingress, no restart, and no host network. Its only eligible decision is disabled-not-rendered. Malformed, unknown, missing, fallback, active, exposure/restart/host-network, freshness/injection/binding, sink-failure, duplicate, and replay cases must close terminally. A deterministic fake may record one scalar decision only; it cannot receive configuration text, root/image, environment, package/registry, credential, target, provider, or operational values. No renderer, target action, staging, proof, training, submission, or aggregation is authorized." },
  { date: "24 AUG 2026", title: "Static target-composition rendering is bounded as a design-only, scalar-safe future contract", status: "PROVISIONAL" as const, icon: FileText, body: "The next design boundary specifies a future static target-composition decision without rendering or writing a configuration. It permits only versioned scalar eligibility labels: configuration eligible, image fresh, synthetic injection prepared, opaque root binding reserved, and activation disabled. It fixes no-port/no-ingress/no-restart/no-host-network controls and rejects raw root/environment/mount/image/package/registry/target/credential/provider/configuration text. The first possible code slice is a pure render-intent/decision policy with a deterministic fake; renderer, Compose file, image binding, package access, target action, proof, training, submission, and aggregation remain out of scope." },
  { date: "24 AUG 2026", title: "Synthetic private-root injection and nonenumerability fixtures pass Agent quality gates", status: "VALIDATED" as const, icon: ShieldCheck, body: "Agent release `19340fc` implements the pure synthetic injection validator, private-field synthetic reference marker, deterministic construction fake, and redaction fixtures. Fixed non-path sentinels cannot reach serialization, inspection, enumeration, errors, snapshots, or decisions. One eligible ready-not-activated request may hand off to a fake; malformed, noneligible, fallback, invalid-sentinel, construction-failure, and replay cases close terminally. Local CI passed 130 TypeScript and four Python tests; Hospital Node Quality Gates `32706887942` succeeded. No raw root, parser, filesystem, deployment, mount, image, package, target, credential, proof, or aggregation action occurred." },
  { date: "24 AUG 2026", title: "Source-only synthetic private-root injection contract is specified before implementation", status: "PROVISIONAL" as const, icon: FileText, body: "The next Agent increment is a pure synthetic injection contract. It accepts only fixed-format non-path sentinels, scalar configuration-ready-not-activated eligibility, one unused injection identifier, and a deterministic construction fake. Sentinels are private/nonenumerable and cannot reach serialization, inspection, own-property enumeration, errors, status, or public records. Malformed, noneligible, fallback, duplicate, and construction-failure cases close terminally before retry or alternate root. The fake emits only scalar results and cannot parse/inject configuration, access filesystem/environment/container/target/Azure/GitHub/registry/Core, or construct a real adapter. No raw reference or external action is authorized." },
  { date: "24 AUG 2026", title: "Target-owned private-root injection adapter is designed as a one-way nonenumerable capability boundary", status: "PROVISIONAL" as const, icon: FileText, body: "The new design permits only a target composition root to hand one opaque private-root reference to an infrastructure injector after scalar configuration intent closes eligible/ready-not-activated. The injector validates bounded syntax internally, retains a private nonenumerable capability, constructs the reviewed durable adapter once, and returns only a persistence port or terminal scalar disabled reason. Applications and policies never receive a root/path/mount/configuration envelope. Compatibility fixtures use synthetic sentinels and verify redaction, one-time handoff, and denial closure without environment/filesystem/container/Azure/GitHub/package/Core access. No raw reference, parser, mount, build, pull, target, proof, or aggregation action occurs." },
  { date: "24 AUG 2026", title: "Private-root configuration intent and fresh-image eligibility contract passes Agent quality gates", status: "VALIDATED" as const, icon: ShieldCheck, body: "Agent release `c3353e6` implements the pure versioned configuration-intent validator and deterministic fake. It evaluates only scalar mode, binding presence, source-release, image-binding, and activation facts. Exact disabled/no-binding/no-image and fully eligible ready-not-activated states are the only disabled outcomes. Missing root, unverified source, stale/mutable/unavailable image, active runtime, mismatches, unsafe disabled state, malformed observations, and replay close terminally. Local CI passed 126 TypeScript and four Python tests; Hospital Node Quality Gates `32706064358` succeeded. No environment/configuration parser, filesystem, deployment, root, image, target, credential, package, proof, or aggregation action occurred." },
  { date: "24 AUG 2026", title: "Source-only private-root configuration intent and fresh-image eligibility contract is specified", status: "PROVISIONAL" as const, icon: FileText, body: "The next Agent increment accepts only scalar mode, binding-presence, source-release, image-binding, and activation facts. Its disabled state requires disabled/no-binding/no-selected-image; private terminal-store intent denies before a fake for missing root, unverified/stale/mutable/unavailable image, active/default runtime, malformed/fallback, and replay claims. It cannot receive a path, mount, environment name, image/package/digest/registry value, target, credential, provider result, process state, or listener fact. Aggregate readout is count-only, and import guards ban every external capability. No injection, configuration, build, pull, target, proof, or aggregation action is authorized." },
  { date: "24 AUG 2026", title: "Concrete private-root configuration and fresh image-mapping design preserves target-only custody", status: "PROVISIONAL" as const, icon: FileText, body: "The new design separates a scalar-safe intent parser from a target-only raw-root injector. The parser accepts only mode, binding-presence, source-release, image-binding, and disabled activation classes; the injector’s private reference cannot cross into application contracts, status, logs, tests, package metadata, or public records. The existing private image candidate is ineligible because it predates these source releases; a later fresh immutable candidate is required. Static rendering must remain disabled/no-port/no-ingress/no-restart and deny path-like public values or active mode. No parser, configuration, image, package, target, credential, proof, training, submission, or aggregation action occurs." },
  { date: "24 AUG 2026", title: "Private-root composition contract passes Agent local and remote quality gates", status: "VALIDATED" as const, icon: ShieldCheck, body: "Agent release `e3070f3` implements the pure versioned private-root composition validator and deterministic fake. It maps absent/invalid/temporary root states, unbound binding, target-runtime claims, destructive rollback, fallback roots, default activation, malformed observations, and replay to terminal retry-disabled outcomes. Only ready-not-activated or binding-removed rollback is disabled, not activated. Local CI passed 122 TypeScript and four Python tests; Hospital Node Quality Gates `32704963862` succeeded. This is source-quality evidence only: no filesystem, environment, deployment configuration, root, mount, target, Azure, provider, credential, package, image, Agent runtime, Core call, proof, training, submission, or aggregation action occurred." },
  { date: "24 AUG 2026", title: "Source-only private-root composition contract is specified before implementation", status: "PROVISIONAL" as const, icon: FileText, body: "The next Agent increment is a pure versioned validator with deterministic fakes. It accepts only allowlisted scalar root, binding, activation, runtime-posture, and rollback classes; it emits only terminal disabled/blocked, retry-disabled decisions. Unknown, fallback, default-activation, target/runtime, destructive-rollback, and replay claims are denied before a fake. Aggregate readout excludes paths, mounts, users, permissions, images, packages, credentials, targets, process results, and listener facts. Static guards prohibit filesystem, environment, Docker/Compose, provider, credential, package, target, Core, proof, training, submission, and aggregation imports." },
  { date: "24 AUG 2026", title: "Private-root composition and rollback design isolates durable state from Agent application and public surfaces", status: "PROVISIONAL" as const, icon: FileText, body: "The new design assigns private-root provisioning to the target operator, opaque binding to deployment configuration, validation/adapter construction to infrastructure composition, and only the persistence port to the application. Composition is opt-in and disabled by default; the existing immutable image is not repurposed. Scalar preflight, no-fallback root denial, aggregate-only observability, conservative record-preserving rollback, and separate new-image/staging/proof gates are defined. No root, configuration, mount, image, Azure, Agent, package, credential, Core, proof, training, submission, or aggregation action occurs in this design increment." },
  { date: "24 AUG 2026", title: "Local durable terminal store adapter passes quality gates after a contained root-redaction correction", status: "VALIDATED" as const, icon: ShieldCheck, body: "Agent release `8ad309f` adds the isolated durable-store adapter and temporary-fixture suite. It validates an injected private root and fixed record, rejects wrong permission/type/symlink/temporary/corrupt states, bounds canonical scalar JSON, and writes through temporary file, flush, atomic promotion, and root flush. An initial local assertion found the injected root capability enumerable in object serialization; it was corrected to private storage before final quality. Local CI passed 118 TypeScript and four Python tests; Hospital Node Quality Gates `32704209672` succeeded. Only test-created local fixtures were touched; no environment/home, target, Azure, provider, credential, package, image, Agent runtime, Core call, proof, training, submission, or aggregation action occurred." },
  { date: "24 AUG 2026", title: "Local-only durable-store adapter implementation increment is authorized with isolated fixture limits", status: "PROVISIONAL" as const, icon: FileText, body: "The implementation is limited to a local Agent-repository infrastructure adapter and its test-created bounded temporary fixtures. The adapter receives a private root capability through its constructor, rejects request-derived paths, and may not read environment configuration, access a developer home, use Azure, query GitHub/registry/Core, bind a port, or project path/content/error data. Required quality includes strict formatting/types, full tests, corruption/permission/symlink/atomic/restart fixtures, and a filesystem-import guard. It remains source-quality work only; every deployment, identity, credential, package, proof, training, submission, and aggregation boundary is blocked." },
  { date: "24 AUG 2026", title: "Durable terminal-diagnostic store adapter is designed with private-root and restart-closure constraints", status: "PROVISIONAL" as const, icon: FileText, body: "The new design confines filesystem access to a future infrastructure adapter, while the composition root alone supplies one prevalidated private state-root capability. A fixed adapter-owned filename holds exactly one canonical scalar terminal record. Strict load/write/flush/atomic-promotion rules, permission and symlink rejection, corruption closure, private temporary cleanup, aggregate-only observability, and local compatibility fixtures are defined. A missing record is the only source-only open condition; any corruption/failure closes. No directory, file, database, configuration, target, credential, package, proof, training, submission, or aggregation action occurs in this design increment." },
  { date: "24 AUG 2026", title: "Terminal diagnostic persistence and restart suppression pass Agent local and remote quality gates", status: "VALIDATED" as const, icon: ShieldCheck, body: "Agent release `4c7d352` implements the pure terminal-record coordinator, restart-safe wrapper, strict scalar validator, and deterministic in-memory fake. It accepts only a version, bounded sequence, blocked state, allowlisted terminal code, replay-suppressed state, and retry disabled; malformed/unknown/non-monotonic/duplicate/forbidden records close. A reloaded closure or append fault suppresses later evaluator work. Local CI passed 114 TypeScript and four Python tests; Hospital Node Quality Gates `32703315914` succeeded. This is source-quality evidence only: no filesystem, database, provider, credential, package, target, proof, or aggregation action occurred." },
  { date: "24 AUG 2026", title: "Source-only terminal persistence and restart boundary is defined for the blocked authorization diagnostic", status: "PROVISIONAL" as const, icon: FileText, body: "The next increment defines an additive scalar-only persistence port and deterministic in-memory fake. A record may contain only version, bounded sequence, blocked state, allowlisted terminal code, replay-suppressed state, and retry disabled; identifiers, account/package/credential/SSO/target/provider/path/free-text facts are rejected. Any reloaded closure conservatively blocks all later evaluations before a fake can run. This proves restart suppression—not durable identity deduplication, permission state, or staging readiness. No filesystem, database, network, provider, credential, package, target, proof, training, submission, or aggregation capability is authorized." },
  { date: "24 AUG 2026", title: "Scalar authorization-diagnostic contract passes Agent local and remote quality gates", status: "VALIDATED" as const, icon: ShieldCheck, body: "Agent release `63359ba` implements the pure versioned scalar validator and deterministic fake. It rejects malformed/unknown input before a fake, maps every allowed state to terminal blocked/no-retry, denies fallback identity and replay, and projects aggregate-only readout; a source guard prohibits provider/runtime imports. Local CI passed 110 TypeScript and four Python tests; Hospital Node Quality Gates `32702812459` succeeded. This is source-quality evidence only: no provider, organization, package, credential, machine account, target, image, Azure, render, Agent start, Core call, proof, training, submission, or aggregation action occurred. The external authorization block remains open." },
  { date: "24 AUG 2026", title: "Source-only scalar authorization-diagnostic contract is specified before implementation", status: "PROVISIONAL" as const, icon: FileText, body: "The next Agent increment is a pure versioned contract that accepts only allowlisted scalar authorization, package-access, machine-identity, target-custody, and identity-path states. Every result is terminally blocked with retry disabled; malformed/unknown input, fallback identity, and replay are denied before a deterministic fake. The design rejects provider responses, account/member references, credentials, locators, scopes, SSO facts, target locations, paths, URLs, headers, bodies, and free text. No configuration, network, browser, package, credential, Azure, target, proof, training, submission, or aggregation capability is authorized." },
  { date: "24 AUG 2026", title: "Read-only organization authorization diagnostic closes as integration-unavailable", status: "BLOCKED" as const, icon: FileWarning, body: "The available repository integration was asked only for the scalar organization-membership class and returned an access denial. No package, image, version, visibility, linkage, inheritance, package access, SSO, member identity, credential, or target fact was read. The result cannot distinguish missing membership privilege from an integration-token limitation and is therefore recorded as organization-authorization-unavailable-to-integration, not a package-access conclusion. No identity switch, browser-role substitution, package query, retry, credential, pull, target render, Agent start, Core call, proof, training, submission, or aggregation action occurred. The next safe slice is source-only diagnostic contracts and deterministic denials." },
  { date: "24 AUG 2026", title: "Dedicated target machine-identity design follows the denied package credential closure", status: "PROVISIONAL" as const, icon: FileText, body: "Official GitHub guidance identifies classic personal access tokens with `read:packages` as the documented Container Registry authentication route; organizations requiring SSO also require token authorization. The observed denial does not establish whether package access, repository linkage, inheritance, SSO, membership, or package state was responsible, so the next design separates a dedicated organization-owned machine identity from personal/browser/repository/Core/worker/callback identities. It defines scalar-only diagnosis states, target-only custody, no fallback/no retry, and staged authorization checks. No second credential attempt, package/image discovery, pull, target render, Agent start, Core call, proof, training, submission, or aggregation action occurs." },
  { date: "24 AUG 2026", title: "Target-only registry credential attempt closes when private-registry authentication is denied", status: "BLOCKED" as const, icon: FileWarning, body: "After the fine-grained form proved unable to offer package-read access, one newly created time-limited package-read credential was attempted for the Azure target. Registry authentication was denied before any package/image inventory, selection, pull, binding, render, target configuration, Agent start, Core call, proof, training, submission, or aggregation action. A credential-observability failure was contained by immediate issuer-side revocation; target registry custody remains absent and the remote session is closed. The attempt is terminal and will not be repeated. A separately documented package-access diagnosis and alternative target-only identity mechanism are required before staging can resume." },
  { date: "24 AUG 2026", title: "Owner authorizes the next target-only credential increment without relaxing identity boundaries", status: "PROVISIONAL" as const, icon: Workflow, body: "The owner authorizes creation and installation of a new narrow package-read identity solely for the Azure target, followed by the documented one inert staging attempt after renewed preflight. The authority does not permit user/browser/repository-operator, Core, ML-worker, callback, package-discovery, public, or package-wide credential reuse. Public evidence will remain scalar-only: target custody and immutable-binding state, never a mechanism, identifier, scope, secret, image/package locator, or target location. No pull, render, Agent start, Core call, proof, training, submission, or aggregation action occurs in this authorization record." },
  { date: "24 AUG 2026", title: "Azure target staging stops pre-route because no target-only private-package pull credential exists", status: "BLOCKED" as const, icon: FileWarning, body: "A presence-only target inspection found no private-registry login; no credential material was read. This closes the staging candidate before route: no target secret creation, human/browser/repository-job credential reuse, package inventory query, image binding/pull, Compose render, Agent start, Core call, proof, training, submission, or aggregation action occurred. The next required input is a newly provisioned target-only package-read credential installed solely on the Azure target and never placed in source, application configuration, Agent process, logs, or public evidence. A fresh scalar Core/aggregation preflight remains mandatory before any one immutable staging attempt." },
  { date: "24 AUG 2026", title: "Inert Azure target-staging composition passes local and remote quality gates", status: "VALIDATED" as const, icon: ShieldCheck, body: "Agent release `c620747` adds an opt-in static staging template that accepts only a target-owned opaque immutable-image reference. It is non-public and inert: no default activation, restart loop, port, build source, secret, volume, Core/identity setting, proof/training/submission/aggregation capability, or image value; the status listener is explicitly disabled. Its policy test rejects public/privileged/host-network/Docker-socket and sensitive-runtime drift. Local CI passed 106 TypeScript and four Python tests; Hospital Node Quality Gates `32697410462` succeeded. No target secret, image pull/binding, Azure configuration, Compose render, Agent start, Core call, proof, training, submission, or aggregation action occurred." },
  { date: "24 AUG 2026", title: "Read-only Azure preflight validates Core safety posture; no Agent target is present", status: "VALIDATED" as const, icon: ShieldCheck, body: "The authoritative target was checked through scalar-only inspection. Docker was available; exactly one labelled Core API and one labelled aggregation-worker candidate were present; internal Core liveness and readiness succeeded; and the worker’s resolved enabled state was disabled. No Agent candidate was present. A broad container-name heuristic was discarded because it could not safely classify the worker; exact Compose labels and a resolved-state classifier supplied the final preflight. No target secret, package pull, image binding, Compose render, Agent start, Core call, proof, training, submission, or aggregation action occurred. Fresh preflight is still required immediately before any future staging." },
  { date: "24 AUG 2026", title: "Separate Azure target-staging boundary is defined before any target contact", status: "PROVISIONAL" as const, icon: FileText, body: "The new dossier splits target staging from image build and proof. It requires a target-only least-privilege private-package pull secret, redacted immutable binding, no published Agent port/ingress/restart/default activation, and immediate authoritative Core health plus aggregation-disabled preflight. The only allowed staging outcome will be an inert non-public process with proof state `not_invoked`. Target secret creation/use, image pull, Azure configuration, Compose render, Agent start, Core calls, training, submission, and aggregation remain blocked in this design increment." },
  { date: "24 AUG 2026", title: "Fresh private synthetic image build succeeds through the BuildKit OCI provenance path", status: "VALIDATED" as const, icon: ShieldCheck, body: "Exactly one fresh manual candidate ran from immutable builder revision `2d38453`. Real checkout, build platform setup, private registry authentication, build/publish, and workflow completion succeeded with fixed BuildKit OCI provenance requested directly in the build. The result is target-unbound. Package inventory remains inaccessible to the available integration, so no package, image, digest, manifest, provenance body, visibility, or locator is publicly claimed. The prior attestation-failed candidate remains terminally closed and was not retried. No Azure action, target binding, Compose render, projection, proof, training, submission, or aggregation action occurred. A separate Azure target-staging dossier is next." },
  { date: "24 AUG 2026", title: "Fresh live builder candidate switches to BuildKit OCI provenance after terminal attestation closure", status: "VALIDATED" as const, icon: ShieldCheck, body: "Agent release `2d38453` removes unavailable GitHub attestation persistence and its unneeded attestations-write and id-token permissions. The same real manual private builder now requests fixed BuildKit OCI provenance mode, retains immutable main SHA context, ephemeral registry authentication, private package target, one-candidate no-cancellation control, bounded timeout, and no automatic trigger. Static tests require the OCI provenance mode and reject the removed attestation action/permissions and unsafe policy drift. Local CI passed all protected guards, 104 TypeScript tests, and four Python tests; Hospital Node Quality Gates `32696462452` succeeded. The fresh candidate has not been dispatched; no new package, image, registry, Azure, target, Compose, projection, proof, training, submission, or aggregation action occurred." },
  { date: "24 AUG 2026", title: "First live private image candidate closes when GitHub attestation storage is unavailable", status: "BLOCKED" as const, icon: FileWarning, body: "One authorized manual synthetic candidate ran from the immutable live-builder release. Real checkout, build platform setup, private registry authentication, and build/publish completed, but the final provenance-attestation persistence step is unavailable for the organization’s current private-repository capability. The candidate closes terminally as live-build-attestation-feature-unavailable: no retry, alternative registry, mutable tag, source/base substitution, target pull, Azure action, target binding, Compose, projection, proof, training, submission, or aggregation action occurred. Package inventory access is unavailable to the current integration, so no public package, image, locator, visibility, or artifact claim is made. A fresh source revision will use fixed BuildKit OCI provenance rather than the unavailable GitHub attestation store." },
  { date: "24 AUG 2026", title: "Real test environment and manual protected-builder workflow pass configuration and quality gates", status: "VALIDATED" as const, icon: ShieldCheck, body: "A real private test environment is configured with zero protection rules, no branch policy, and zero environment secrets under the documented single-admin exception. Agent release `ffc588b` adds the separate Protected Builder Live Test workflow: manual dispatch only, an explicit false-by-default execution input, main-branch guard, one-candidate concurrency without cancellation, bounded timeout, minimal job permissions, full-SHA action pins, ephemeral registry authentication, immutable SHA-tag construction, and provenance attestation. Its static test rejects unsafe policy drift. Local CI passed all protected guards, 104 TypeScript tests, and four Python tests; Hospital Node Quality Gates `32696009922` succeeded. The workflow has not been dispatched; no package, image, registry publication, Azure action, target binding, Compose, projection, proof, training, submission, or aggregation action occurred." },
  { date: "24 AUG 2026", title: "Owner authorizes a real live-test builder, private publication, Azure staging, and bounded synthetic proof path", status: "PROVISIONAL" as const, icon: Workflow, body: "The owner explicitly changes the protected-builder objective from a no-op test control to a functioning live-test path: real private environment, manual builder workflow, private package, one bounded image build/push, later real Azure staging, and one bounded synthetic proof. The single-admin exception remains reduced assurance and is not represented as independent review. Every operation still has a separate evidence gate; no clinical data, public Agent API, broad identity, direct provider/storage access, training, submission, or aggregation is permitted. No environment, workflow, package, credential, image, registry route, Azure action, target binding, Compose render, projection, proof, training, submission, or aggregation action occurs in this decision." },
  { date: "24 AUG 2026", title: "Owner authorizes a bounded test-only single-admin protected-builder exception", status: "PROVISIONAL" as const, icon: Workflow, body: "The owner explicitly authorizes a reduced-assurance test exception because independent reviewer/custodian roles are unavailable. The exception permits only one private test environment, one private repository-linked package boundary, a disabled-by-default manual workflow, one exact source/base candidate, no environment secrets or long-lived credential, minimal ephemeral permissions, SHA-pinned action classes, provenance, redacted audit, and terminal no-retry closure. It is not production segregation of duties and cannot support hospital integration, clinical data, target deployment, proof, training, submission, or aggregation. No environment, workflow, package, credential, image, registry route, Azure action, target binding, Compose render, projection, proof, training, submission, or aggregation action occurs in this decision." },
  { date: "24 AUG 2026", title: "Protected-builder configuration blocks on missing independent reviewer and custodian", status: "BLOCKED" as const, icon: FileWarning, body: "Read-only inspection confirms repository-level administrator authority, an active repository, and a tracked container-build source. It does not establish provider configuration readiness: organization-plan capability is unverified, team inventory is inaccessible to the available integration, and a separate organization view shows one visible member with no configured teams. The same owner identity cannot safely dispatch and independently review a protected build, so independent reviewer/custodian availability is terminally blocked. No workaround identity, environment, workflow, package, credential, image, registry route, Azure action, target binding, Compose render, projection, proof, training, submission, or aggregation action was created or invoked." },
  { date: "24 AUG 2026", title: "Read-only protected-environment feasibility record closes blocked before provider configuration", status: "BLOCKED" as const, icon: FileWarning, body: "Read-only repository and Actions inspection projects only scalar facts: the Agent repository is private and active, default workflow-token permissions are read-only, automatic pull-request approval is disabled, and the environment inventory is readable with zero configured environments. There is therefore no evidenced reviewer/custodian assignment, no-bypass rule, branch restriction, protected secret boundary, environment capability, or private-package policy. The result is feasibility-blocked, not configuration approval. No environment, workflow, package, credential, image, registry route, Azure action, target binding, Compose render, projection, proof, training, submission, or aggregation action was created, changed, or invoked." },
  { date: "24 AUG 2026", title: "Source-only protected-builder workflow-policy validator passes quality gates", status: "VALIDATED" as const, icon: ShieldCheck, body: "Agent release `2b0a018` validates only fixed scalar policy classes for manual activation, independent non-self review/no bypass, private repository-linked package posture, exact least-privilege permissions, full SHA action pins, one-candidate/no-cancellation control, bounded timeout, and target-unbound state through a deterministic review fake. Invalid, forbidden, expired, duplicate, replayed, denied, revoked, quarantined, and role-conflicted values close terminally; only policy-validated-source-only can succeed; public readout is aggregate only. Local CI passed all protected import guards, strict TypeScript, 101 TypeScript tests, and four Python tests; Hospital Node Quality Gates `32694621127` succeeded. No environment, workflow, package, credential, image, registry, Azure, target, Compose, projection, proof, training, submission, or aggregation action exists." },
  { date: "24 AUG 2026", title: "Protected-environment policy keeps the selected builder manual, reviewed, and non-executable", status: "PROVISIONAL" as const, icon: Workflow, body: "The documentation-only policy defines a separate protected environment with feasibility verification before configuration, independent non-self review, no administrator bypass, manual-only activation, immutable one-candidate freshness checks, no concurrent cancellation of an approved candidate, minimal job permissions, SHA-pinned actions, strict input denial, redacted audit, and terminal cancellation/revocation. It records GitHub platform limitations without downgrading the controls: environment protection availability and private-package posture must be verified by an administrator before any configuration. No environment, workflow, package, token, image, registry route, Azure action, target binding, Compose render, projection, proof, training, submission, or aggregation action occurs." },
  { date: "24 AUG 2026", title: "Repository-hosted protected builder selects GitHub Actions and a private container registry boundary", status: "PROVISIONAL" as const, icon: Workflow, body: "The documentation-only control-plane decision selects a separate, disabled-by-default GitHub Actions builder and private repository-linked GitHub Container Registry package. It requires one-candidate authorization, a protected environment, SHA-pinned actions, job-scoped least privilege, immutable source/base facts, digest-bound provenance, redacted logs, candidate quarantine, credential revocation, private package visibility, and renewed target separation. The existing quality workflow remains separate. No workflow, package, token, image, registry route, Azure action, target binding, Compose render, projection, proof, training, submission, or aggregation action is configured or invoked." },
  { date: "24 AUG 2026", title: "Reconciled source-only action consumption passes protected quality gates", status: "VALIDATED" as const, icon: ShieldCheck, body: "The clean local Agent checkout was fast-forwarded from stale `1d56fdd` to authoritative `b50fdc9` before new work; the restored baseline CI passed 91 TypeScript and four Python tests. Agent release `909470e` adds strict admission/envelope/action/freshness binding through deterministic pre-route and retirement fakes. It permits only in-memory route-not-started, route-started-placeholder, and post-route-closed states; makes malformed, mismatch, expiry, duplicate, replay, role conflict, denial, revocation, quarantine, retirement, and ordering failure terminal; exposes aggregate counts only; and adds an action-consumption import guard. Local CI passed all protected guards, 96 TypeScript tests, and four Python tests; Hospital Node Quality Gates `32693437721` succeeded. No instruction, credential, builder, registry, Docker, image, Azure, target, Compose, projection, proof, training, submission, or aggregation action exists." },
  { date: "24 AUG 2026", title: "One-candidate execution-action decision retains pre-route and post-route external controls", status: "PROVISIONAL" as const, icon: Workflow, body: "The new documentation-only action decision defines a non-transferable one-candidate consumption rule, independent freshness recheck, strict credential-custody handoff prohibition, redacted route declaration, pre-route stop rules, post-route failure publication, candidate retirement, no retry or alternate-channel behavior, and renewed no-target-deployment separation. Its route-started state is a present-scope placeholder only and asserts no external action. It consumes no envelope, identifies no candidate or operator, and creates/uses no credential; no builder, registry, Docker, image, Azure, target, Compose, projection, proof, training, submission, or aggregation action occurs." },
  { date: "24 AUG 2026", title: "Source-only one-candidate authorization envelope passes protected quality gates", status: "VALIDATED" as const, icon: ShieldCheck, body: "Agent release `b50fdc9` adds strict admission/readiness/request/freshness binding through deterministic policy, execution, registry, and custody fakes. It returns only ready-not-executable after all fakes agree; suppresses later reviews after closure; makes malformed, mutable, invalid, mismatch, expiry, duplicate, replay, role/state error, denial, revocation, quarantine, and a source-only post-route-placeholder failure terminal; permanently blocks ready-request reuse after the placeholder; exposes aggregate counts only; and adds an authorization-envelope source-import guard. Local CI passed formatting, all protected import guards, strict TypeScript, 91 TypeScript tests, and four Python tests; Hospital Node Quality Gates `32691244949` succeeded. No instruction, candidate, credential, builder, registry, Docker, image, Azure, target, Compose, projection, proof, training, submission, or aggregation action exists." },
  { date: "24 AUG 2026", title: "One-candidate external execution envelope preserves the hard external-action block", status: "PROVISIONAL" as const, icon: Workflow, body: "The new documentation-only envelope requires one immutable candidate admission, independent policy/execution/registry/custody reviews, bounded approval freshness, a credential-readiness attestation without material, renewed no-target-deployment separation, terminal denial/revocation/quarantine, append-only redacted audit, and post-route failure publication before any later retry. Its only ready state is explicitly non-executable and cannot issue a build instruction. It identifies no candidate or operator and creates/uses no credential; no builder, registry, Docker, image, Azure, target, Compose, projection, proof, training, submission, or aggregation action occurs." },
  { date: "24 AUG 2026", title: "Source-only execution-readiness record passes protected quality gates", status: "VALIDATED" as const, icon: ShieldCheck, body: "Agent release `8d3c415` composes strict immutable admission/readiness binding through deterministic policy, execution-approval, registry, and credential-custody fakes, plus an injected scalar clock. It validates before any seam; suppresses later fakes after policy/execution/registry closure; makes ready-not-authorized explicitly non-executable; treats malformed, mismatch, expiry, duplicate, replay, role/state error, revocation, and quarantine as terminal; exposes aggregate counts only; and adds a readiness source-import guard. Local CI passed formatting, protected filesystem, builder-orchestration, and readiness guards, strict TypeScript, 86 TypeScript tests, and four Python tests; Hospital Node Quality Gates `32690774152` succeeded. No credential, builder, registry, Docker, image, Azure target, Compose render, projection, proof, training, submission, or aggregation action exists." },
  { date: "24 AUG 2026", title: "External builder execution readiness keeps credentials and target access outside the Agent", status: "PROVISIONAL" as const, icon: Workflow, body: "The new documentation-only readiness record defines mutually isolated policy, execution-approval, registry-release, and credential-custodian classes; a one-candidate evidence state machine; immutable scalar anchors; append-only redacted readout; independent credential creation/rotation/revocation; expiry, disagreement, quarantine, revocation, and platform-failure closure; disposable non-root builder constraints; candidate-scoped registry release; source/base pin re-verification; audit redaction; and no-target-deployment re-attestation. It appoints no operator and does not create/use a credential or contact a builder, registry, Azure, or target. The Agent remains source-validated, image-free, and target-unbound; actual execution remains separately blocked." },
  { date: "24 AUG 2026", title: "Source-only protected-builder admission orchestration passes protected quality gates", status: "VALIDATED" as const, icon: ShieldCheck, body: "Agent release `8052024` composes strict scalar admission binding through injected deterministic policy-authority and registry-approval fakes. The one-request orchestrator suppresses registry evaluation on invalid input or policy denial; returns an explicitly non-executable `authorized_source_only` outcome only after both fake attestations; treats quarantine, invalid role/state, mismatch, duplicate, replay, malformed, and unknown fields as terminal; exposes aggregate counts only; and adds a source guard against Node, process, Docker, cloud SDK, and HTTP-client imports. Local CI passed formatting, protected filesystem and builder-import guards, strict TypeScript, 81 TypeScript tests, and four Python tests; Hospital Node Quality Gates `32690357676` succeeded. No credential, builder, registry, Docker, image, Azure target, Compose render, projection, proof, training, submission, or aggregation action exists." },
  { date: "24 AUG 2026", title: "Protected-builder authorization record separates custody from Agent and target execution", status: "PROVISIONAL" as const, icon: Workflow, body: "The new documentation-only record names separate opaque policy-authority, protected-builder, and registry-release-approver classes; requires immutable admission facts, an approved base/runtime class, isolated non-root build constraints, one-candidate future credential custody outside the image/runtime, allowlisted redacted evidence, independent review, terminal quarantine, and policy-side rollback. It also requires a `no_target_deployment_attested` conclusion before any future builder execution. The Agent remains source-validated, image-free, and target-unbound. No credential is minted or used, and no builder, Docker, registry, image, Azure target, Compose render, projection, proof, training, submission, or aggregation action occurs." },
  { date: "24 AUG 2026", title: "Source-only image admission and release-mapping contracts pass protected quality gates", status: "VALIDATED" as const, icon: ShieldCheck, body: "Agent release `a4bf11a` adds pure versioned build-admission, immutable target-unbound release-mapping, quarantine contracts, and an in-memory aggregate-only release book. It validates exact source/lockfile/runtime/policy/quality/import-guard facts; rejects tag, registry, target, projection, credential, and unknown fields; refuses source mismatch, invalid state, duplicate mapping, and quarantined candidate without retry; and keeps source/digest values out of public book serialization. Local CI passed formatting, the production-source filesystem-import guard, strict TypeScript, 77 TypeScript tests, and four Python tests; Hospital Node Quality Gates `32689772080` succeeded. A synthetic digest is contract input only: no Docker, image, registry, Azure target, Compose render, projection, token/provider operation, proof, training, submission, or aggregation exists." },
  { date: "24 AUG 2026", title: "Protected image-build decision keeps the Agent source release image-free and target-unbound", status: "PROVISIONAL" as const, icon: Workflow, body: "The new decision defines an opaque protected builder, exact source/dependency/runtime/policy admission facts, non-root/no-listener/no-proof-default image requirements, a production-source import-guard rerun, immutable digest-only release mapping, redacted build outcomes, and candidate quarantine. It expressly authorizes no container build, daemon, registry contact, push, tag, Azure staging, Compose render, projection, or proof. The current Agent remains source-validated and image-free. The only next executable slice is a source-only admission/mapping validator with deterministic tests; any protected build needs a separate later authorization." },
  { date: "24 AUG 2026", title: "Azure test-target binding decision keeps the source release deliberately unbound", status: "PROVISIONAL" as const, icon: Workflow, body: "The new decision defines immutable source, dependency, image, policy, quality, and deployment admission facts for any future protected Azure test-target release. The current source release has only source/quality facts and is therefore explicitly `source_validated_target_unbound`: no image, binding, projection, Agent target, Compose render, or invocation exists. A strict state machine, terminal denial behavior, target mapping quarantine, and aggregate-safe verification prevent mutable-tag, profile, identity, or target reuse. The next safe action is a separate image-build/release-mapping design; it cannot build, push, stage, or invoke anything." },
  { date: "24 AUG 2026", title: "Authorized source-only Node secret-read edge passes source and protected quality gates", status: "VALIDATED" as const, icon: ShieldCheck, body: "Agent release `1982f5d` adds the authorized source-only Node secret-read edge and its single production `node:fs` adapter. A private protected binding holds the location; Linux-only non-following open and close-on-exec support fail closed; metadata is checked before and after bounded read; the edge clears its owned buffer and closes on every path; and only scalar errors cross the boundary. The new production-source import guard allows `node:fs` only in this module. All tests use injected syscall doubles and cover normal clearing/close plus open/kind/access/size/change/short-read/read/close/unknown failures, redaction, and no retry. Local CI passed formatting, the import guard, strict TypeScript, 74 TypeScript tests, and four Python tests; Hospital Node Quality Gates `32689071998` succeeded. No real projection, secret read, provider/token operation, image, Azure target, Compose render, proof, training, submission, or aggregation occurred." },
  { date: "23 AUG 2026", title: "Owner authorizes the bounded source-only Node secret-read edge", status: "PROVISIONAL" as const, icon: Workflow, body: "The project owner explicitly authorized one source-local, Azure test-target Node secret-read edge after the fake syscall review. The authorization is limited to the fixed protected-binding implementation and injected syscall-double tests. It excludes generic filesystem access, caller paths, environment discovery, secret output, provider exchange, token acquisition, image release, Azure staging, Compose render, preflight, proof, training, submission, aggregation, and public exposure. The source edge remains fail-closed, redacted, nonpersistent, and rollbackable at source level; after its quality evidence, a new decision will still be required for any image or target activity." },
  { date: "23 AUG 2026", title: "Fake descriptor-first syscall port passes source and protected quality gates", status: "VALIDATED" as const, icon: ShieldCheck, body: "Agent release `1d56fdd` adds a deterministic four-operation descriptor-first syscall double and fake identity bridge: non-following open, metadata inspect, bounded read, re-inspect, and close over opaque in-memory handles. It exposes aggregate counts only and accepts no path, mount, descriptor number, secret, environment, or Node filesystem primitive. Tests prove the closed sequence plus unsupported platform, open/metadata/kind/access/size/change/short-read/read/close/unknown/handle failures, redaction, no cache/watch/retry, and exactly-one close after open. Local CI passed formatting, strict TypeScript, 71 TypeScript tests, and four Python tests; Hospital Node Quality Gates `32668776028` succeeded. This is not a Node filesystem adapter: no projection, secret, provider, image, Azure target, Compose render, proof, training, submission, or aggregation exists. A separate audited implementation decision is now required." },
  { date: "23 AUG 2026", title: "Concrete Node filesystem secret-read edge is designed as descriptor-first and fail-closed", status: "PROVISIONAL" as const, icon: Workflow, body: "The new review confines any future Node filesystem primitive to one private platform port behind the existing protected binding. It specifies fixed binding ownership, non-following descriptor-first open, metadata validation, bounded one-read and post-read same-object recheck, `finally` close, opaque one-use lease, edge-owned buffer clearing, scalar failures, and redacted observability. Unsupported primitives fail closed; no path-following, `readFile`, generic filesystem helper, cache, watcher, retry, or fallback identity is allowed. The next executable slice is a deterministic syscall-port fake, not a Node import or projection open. Image, Azure target, Compose render, preflight, proof, training, submission, and aggregation remain blocked." },
  { date: "23 AUG 2026", title: "Fake protected-projection lifecycle passes source and protected quality gates", status: "VALIDATED" as const, icon: ShieldCheck, body: "Agent release `041c386` adds an injected fake protected-projection port, opaque one-use lease, explicit disposal, and bridge into the fake identity seam. It accepts only metadata facts and exposes aggregate lifecycle counts; no path, mount, descriptor, reference, secret, environment, provider option, or Node filesystem capability exists. Tests cover the closed lifecycle plus absent/wrong-kind/unsafe-access/zero/oversize/change/read/refusal/duplicate-use/disposal/restart/unknown-error cases, no cache/watch/retry, and redaction. Local CI passed formatting, strict TypeScript, 68 TypeScript tests, and four Python tests; Hospital Node Quality Gates `32668418777` succeeded. This is not a projection open or concrete filesystem adapter; image, Azure target, Compose render, proof, training, submission, and aggregation remain absent." },
  { date: "23 AUG 2026", title: "Protected concrete secret-read edge is designed as a closed, nonserializable lifecycle", status: "PROVISIONAL" as const, icon: Workflow, body: "The new design isolates a future deployment-owned projection edge from provider exchange and application code. It fixes one reference class, regular-file-only and owner-only policy, bounded one-read behavior, opaque internal lease, `finally` disposal, scalar failures, no cache/watch/retry/fallback identity, and no secret/path/mount/owner/token/provider projection. Rotation and restart are outer deployment events; failed disposal is terminal. The next executable slice remains an injected fake metadata/read/closure port only—no Node filesystem import or projection open. Image binding, Azure staging, Compose render, preflight, proof, training, submission, and aggregation remain blocked." },
  { date: "23 AUG 2026", title: "Fake secret-material and identity-exchange ports pass source and protected quality gates", status: "VALIDATED" as const, icon: ShieldCheck, body: "Agent release `fd43813` adds deterministic fake secret-material and identity-exchange ports plus a closed fake secret-backed workload-token source. Material contains only narrow metadata; the exchange returns an opaque fake token only to the typed Core-client seam. Tests cover one read/exchange, wrong audience before material access, invalid kind/mode/length/shape, not-before, expiry, policy denial, unavailable exchange, invalid response, redaction, no cache, and no retry. Local CI passed formatting, strict TypeScript, 65 TypeScript tests, and four Python tests; Hospital Node Quality Gates `32668003652` succeeded. No secret byte/path/mount/environment read, OIDC/provider contact, token acquisition, image, Azure target, Compose render, proof, training, submission, or aggregation exists. The next boundary remains design-only: a protected concrete secret-read edge." },
  { date: "23 AUG 2026", title: "Concrete Agent secret-source adapter is designed as a protected, nonpersistent boundary", status: "PROVISIONAL" as const, icon: Workflow, body: "The new design fixes a narrow future identity seam: a protected deployment-owned reference class, literal `fedagg-hospital-node` audience, bounded one-attempt read/exchange, scalar expiry/rotation/policy failures, no cache or retry, and no secret/reference/token/provider projection. Only the later concrete adapter may resolve a fixed private projection, behind the closed workload-token source interface; application code, runner, workspace, channel, and Core client do not obtain generic identity or filesystem capability. The first executable slice remains deterministic fake material/provider ports only. No secret read, token acquisition, OIDC/provider contact, image binding, Azure staging, Compose render, proof, training, submission, or aggregation is authorized." },
  { date: "23 AUG 2026", title: "All four fake Agent ports close one scalar one-shot lifecycle under protected quality gates", status: "VALIDATED" as const, icon: ShieldCheck, body: "Agent release `8020cd6` composes the fake workload-token source, scripted closed request port, pathless workspace filesystem, and fixed private proof channel through the existing injected factory and one-shot runner. It proves one generated in-memory handoff → lease → intent → full-body stream → materialization → scalar result path, then explicit workspace/channel closure; its terminal matrix covers token, transport, workspace, result-write, and cleanup failure without retry or sensitive projection. A runner change ensures a failed result write propagates instead of entering the executor-failure branch for a second write. Local CI passed formatting, strict TypeScript, 62 TypeScript tests, and four Python tests; Hospital Node Quality Gates `32667511970` succeeded. This remains fake-only: no secret, OIDC, HTTP/socket, host/tmpfs filesystem, image, Azure target, Compose render, proof, training, submission, or aggregation exists." },
  { date: "23 AUG 2026", title: "Fake-first fixed private proof channel passes source and protected quality gates", status: "VALIDATED" as const, icon: ShieldCheck, body: "Agent release `36a9c18` adds `FakeScriptedPrivateProofChannelFilesystem`, a fixed opaque handoff/result double with one handoff read, exclusive scalar result write, bounded serialized records, aggregate-only state, scripted refusal, and explicit cleanup. Tests cover valid closure, absent/oversized/malformed handoff, duplicate and refused result write, cleanup refusal, no executor invocation for malformed handoff, and serialization redaction of assignment/digest/payload facts. Local CI passed formatting, strict TypeScript, 59 TypeScript tests, and four Python tests; Hospital Node Quality Gates `32667220978` succeeded. No host filesystem/path/directory, socket, Core/Azure/storage/provider contact, image, target, Compose render, proof, training, submission, or aggregation exists. The four fake port families are now separately evidenced; next is fake-only composition, not target runtime work." },
  { date: "23 AUG 2026", title: "Fake-first private-workspace filesystem port passes source and protected quality gates", status: "VALIDATED" as const, icon: ShieldCheck, body: "Agent release `cf6c2cb` adds `FakeScriptedPrivateWorkspaceFilesystem`, an injected root-state and operation-refusal double for the existing pathless workspace port. It supports opaque exclusive temporary references, append, close, promotion, removal, and discard while immediately dropping appended bytes and exposing only aggregate lifecycle counts. Tests cover normal closure, denied root, duplicate allocation, append/close/promote failure, cleanup refusal, and serialization that omits receipt, nonce, and fixture bytes. Local CI passed formatting, strict TypeScript, 56 TypeScript tests, and four Python tests; Hospital Node Quality Gates `32667019348` succeeded. No Node filesystem, host path/mount, directory enumeration, image, Azure target, proof, training, submission, or aggregation exists. This is a fake port, not a private tmpfs implementation." },
  { date: "23 AUG 2026", title: "Fake-first closed Core request port passes source and protected quality gates", status: "VALIDATED" as const, icon: ShieldCheck, body: "Agent release `6dc9f9e` adds `FakeScriptedHospitalNodeCoreRequestPort`, a finite injected-script double for only the existing `read_intent` and `model_stream` union. It exposes aggregate operation counts and remaining steps only; does not retain URL, authorization, idempotency key, token, request body, or response body; and uses private fields so serialized state omits test token, assignment, and checksum. Tests cover the valid two-operation path plus operation mismatch, exhaustion, redirect, partial, multipart, encoded, missing-length, and simulated refusal without retry. Local CI passed formatting, strict TypeScript, 53 TypeScript tests, and four Python tests; Hospital Node Quality Gates `32666788271` succeeded. No fetch/socket, Core/Azure/storage/provider call, configuration/secret/token read, image, Azure target, proof, training, submission, or aggregation exists. This is a fake port, not a real transport." },
  { date: "23 AUG 2026", title: "Fake-first Agent workload-token source passes source and protected quality gates", status: "VALIDATED" as const, icon: ShieldCheck, body: "Agent release `7a29d09` adds `FakeHospitalNodeWorkloadTokenSource`, an injected-material deterministic double compatible with the closed typed Core client seam. It accepts only the literal hospital-node audience; validates version, bounded issuance ID, expiry, and opaque token material internally; returns a token only to the client seam; rejects missing, expired, malformed, wrong-audience, and replayed material without retry; and uses private fields so source serialization exposes neither token nor issuance value. Local CI passed formatting, strict TypeScript, 50 TypeScript tests, and four Python tests; Hospital Node Quality Gates `32666544019` succeeded. No secret read, OIDC library/request, token cache, network, filesystem, image, Azure target, proof, training, submission, or aggregation exists. This is a fake adapter, not a protected identity integration." },
  { date: "23 AUG 2026", title: "Target-safe Agent runtime-port bindings are designed before implementation", status: "PROVISIONAL" as const, icon: Workflow, body: "The next design splits target-safe binding into four narrow port families: a separate hospital-node token source; a closed two-operation HTTPS request port; a pathless Agent-private workspace filesystem; and a fixed, directional handoff/result channel filesystem. Each must be owned by the protected outer composition boundary, take only typed values, preserve existing scalar result/closure ownership, and prohibit generic transport/filesystem capability, secret/config projection, token/path/body/locator output, automatic retry, trainer/update/submission work, or aggregation. The roadmap now requires isolated fake-first adapter slices and quality evidence before any image release binding, Azure source staging, Compose render, or proof. Azure still has no Agent source, image, executable Core runner, or composite profile; the pre-route block remains active." },
  { date: "23 AUG 2026", title: "Injected-only Agent runtime composition passes source and protected quality gates", status: "VALIDATED" as const, icon: ShieldCheck, body: "Agent release `7983502` implements the reviewed injected-only composition factory. Explicit enablement, prevalidated configuration, and closed token/request/private-filesystem/lease/repository ports construct only the existing typed Core client, pathless workspace, and handoff orchestration; the existing one-shot runner retains opaque handoff read, exactly-one scalar result write, and channel cleanup. Deterministic fakes prove refusal before any port call, one verified scalar result, terminal token/transport/workspace failures, and no retry or sensitive result projection. Local CI passed formatting, strict TypeScript, 47 TypeScript tests, and four Python tests; Hospital Node Quality Gates `32666107849` succeeded. This adds no concrete OIDC/HTTP/filesystem/channel binding, image build/release, Agent target source, Azure composite render, fixture, route, proof, training, submission, or aggregation. The pre-route block remains active." },
  { date: "23 AUG 2026", title: "Concrete Agent runtime adapter composition is reviewed as injected-only", status: "PROVISIONAL" as const, icon: Workflow, body: "The next Agent slice may compose the existing typed Core client, pathless workspace, private channel, and one-shot runner only through injected token, request, filesystem, and channel ports plus prevalidated configuration. The factory must be pure: no environment access, OIDC library/secret read, fetch/provider client, Node filesystem, channel discovery, Docker command, image binding, fixture, database, training, submission, or aggregation. Its fake-only tests will cover refusal-before-port-use, one handoff/result lifecycle, scalar terminal failures, and no retries. Real adapters, image build/release, Azure rendering, and proof remain separate gates." },
  { date: "23 AUG 2026", title: "Protected Agent proof-image source passes quality without building or running a container", status: "VALIDATED" as const, icon: ShieldCheck, body: "Agent release `714480d` adds a separate proof-runner Dockerfile and scalar preflight command. The source uses Node 22, frozen lockfile installation, an unprivileged node user, and an explicit non-daemon preflight command; it declares no EXPOSE/ENTRYPOINT, does not invoke the private runner, and was not built, pushed, tagged, deployed, or run. Its versioned scalar readout omits origins, secret/workspace references, limits, handoff/result facts, tokens, bodies, fixtures, providers, and diagnostics. Local CI passed formatting, strict TypeScript, 44 TypeScript tests, and four Python tests; Hospital Node Quality Gates `32665704597` succeeded. No concrete identity/client/channel/workspace composition, Azure topology render, fixture, route, or proof exists." },
  { date: "23 AUG 2026", title: "Protected Agent image and entrypoint are designed to validate only, not invoke", status: "PROVISIONAL" as const, icon: Workflow, body: "The next Agent source slice defines a separate proof-runner Dockerfile and a non-invoking entrypoint. It may validate the existing opt-in protected configuration and emit a versioned scalar ready/refused readout, but it will not acquire identity, construct a Core client, open a channel, read/write proof records, use a workspace, invoke the private runner, or contact any service. The image contract requires a frozen-lockfile Node 22 build, non-root process, explicit non-daemon command, no public port, no default activation, and no build/push/deployment activity. A ready result means structural configuration only—not image availability, secret validity, runtime composition, Core connectivity, or proof readiness." },
  { date: "23 AUG 2026", title: "Core source-only runner readout passes quality and protected deployment without runtime authority", status: "VALIDATED" as const, icon: ShieldCheck, body: "Core release `550ebf8` adds a pure source-only entrypoint that accepts only explicit enablement, caller-supplied UTC time, and a preconstructed coordinator. It returns a versioned scalar readout—completed with an existing allowlisted result, or timed-out/refused without diagnostics—and refuses disabled/invalid input before coordinator use. Three deterministic tests cover projection, refusal-before-use, timeout/refusal, and no retry. Local `pnpm check` passed formatting, lint, strict TypeScript, 82 TypeScript tests with 18 integration tests skipped, and nine Python tests; Core Quality Gates `32665135582` and protected Azure deployment `32665135576` succeeded. This is not a runtime runner: no configuration, channel, fixture, clock, filesystem, database, transport, identity, provider, image, profile, token, route, workspace, training, submission, or aggregation exists." },
  { date: "23 AUG 2026", title: "Source-only Core runner entrypoint is designed without runtime authority", status: "PROVISIONAL" as const, icon: Workflow, body: "The next Core slice will accept explicit opt-in, caller-supplied UTC time, and a preconstructed coordinator, then return a versioned scalar readout of completed, timed-out, or refused. It will deny disabled or invalid input before coordinator use and will not project handoffs/results, assignment facts, digests, paths, bytes, headers/bodies, provider data, locators, tokens, secrets, or free-text diagnostics. As a pure application entrypoint, it cannot construct a filesystem, channel, fixture, timer, database, transport, identity, provider, Compose profile, container, or public route. Its tests will use deterministic coordinator fakes only; this design does not authorize an image, target runner, Azure render, or proof." },
  { date: "23 AUG 2026", title: "Deterministic context and wait ports close locally without retry or runtime capability", status: "VALIDATED" as const, icon: ShieldCheck, body: "Core release `bde985a` implements a fake-only generated-context port that validates one supplied handoff, issues it once, and permits only one correlated close/abort. Its bounded wait port accepts only a positive limit and returns continued, timed-out, or interrupted scalar facts without a timer, scheduler, retry queue, configuration, identity, network, persistence, storage/provider, fixture, route, listener, or worker. The coordinator now cleans channel records on timeout/interruption without retry. Local `pnpm check` passed formatting, lint, strict TypeScript, 79 TypeScript tests with 18 integration tests skipped, and nine Python tests; Core Quality Gates `32660088251` and protected Azure deployment `32660088268` succeeded. No fixture, executable runner, Agent image, Azure profile/render, token, route, workspace, training, submission, provider interaction, or aggregation exists." },
  { date: "23 AUG 2026", title: "Generated-context and wait ports are split before any Core runner exists", status: "PROVISIONAL" as const, icon: Workflow, body: "The next Core-only design separates the last two source-composition inputs: a generated-context port may return one prevalidated scalar handoff and close/abort through scalar callbacks, while a wait port may advance a positive bounded local poll count and surface only completion, timeout, or interruption. Both start with deterministic fakes. Neither may create a fixture, query persistence, access storage/provider services, read environment or secrets, acquire identity, open a network connection, construct an Agent channel, schedule retry, expose a listener, or invoke Compose. The next implementation proves local binding and denial behavior only; no target runner or proof is authorized." },
  { date: "23 AUG 2026", title: "Core tmpfs coordinator composition joins only released local-safe ports", status: "VALIDATED" as const, icon: ShieldCheck, body: "Core release `bda376a` combines the fixed-root tmpfs filesystem adapter, fixed-operation file channel, and explicit coordinator factory while requiring injected generated-context and bounded wait ports. Its fake-only test proves a single scalar pre-route result, one closure, and removal of both fixed records; it creates no fixture, identity, network, configuration, provider, database, or runner capability. Local `pnpm check` passed formatting, lint, strict TypeScript, 77 TypeScript tests with 18 integration tests skipped, and nine Python tests; Core Quality Gates `32659163268` and protected Azure deployment `32659163281` succeeded. No executable Core runner, Agent image, composite Azure profile/render, fixture, token, route, workspace, training, submission, provider access, or aggregation exists." },
  { date: "23 AUG 2026", title: "Core tmpfs edge adapter release is repaired after a pre-build lockfile block", status: "VALIDATED" as const, icon: ShieldCheck, body: "Core `9e7128d` adds an isolated tmpfs channel edge package owning two fixed internal roots and filenames. It rejects unsafe roots, bounds records to 4 KiB, uses exclusive 0600 writes, and exposes no path selection or directory enumeration to application code; deterministic fake-node tests passed within local quality (76 TypeScript tests, 18 integration tests skipped, nine Python tests). Its first remote quality and deployment workflows failed before build because the new workspace dependency was absent from the frozen lockfile. Lockfile-only release `9ef36ae` repaired that pre-route packaging condition; local quality passed again, Core Quality Gates `32658551164` and protected Azure deployment `32658551195` succeeded. No target tmpfs mount, runner, Agent image, composite render, fixture, token, route, training, submission, provider access, or aggregation occurred." },
  { date: "23 AUG 2026", title: "Core private file-channel adapter fixes the two-record cleanup boundary", status: "VALIDATED" as const, icon: ShieldCheck, body: "Core release `91fa453` adds a strict pathless file-channel adapter with only fixed logical handoff/result operations. It can confirm an empty private channel, exclusively write one validated handoff, parse one result, and remove both records during terminal closure; it cannot enumerate directories, accept paths, read environment, resolve secrets, or construct network, identity, provider, database, fixture, or runner behavior. Deterministic in-memory tests deny stale content and malformed result serialization. Local `pnpm check` passed formatting, lint, strict TypeScript, 74 TypeScript tests with 18 integration tests skipped, and nine Python tests; Core Quality Gates `32657908322` and protected Azure deployment `32657908327` succeeded. This is not yet the concrete Azure tmpfs adapter, runner, profile, render, or proof." },
  { date: "23 AUG 2026", title: "Azure preflight confirms healthy Core but no target Agent composite to render", status: "BLOCKED" as const, icon: Workflow, body: "A read-only Azure preflight observed public Core liveness and readiness at HTTP 200 plus the aggregation worker’s explicit disabled marker. It also found no Hospital Node Agent source directory, protected composite topology file, or running proof runner in the target layout. Since no deployable Agent image binding or composite profile exists there, an Azure Compose render would not be meaningful and was not attempted. This is a safe pre-route block—not a failed proof. No fixture, secret read, Agent token, lease, intent, stream, workspace write, route request, training, submission, provider interaction, or aggregation occurred." },
  { date: "23 AUG 2026", title: "Core composition factory requires explicit bounded proof wiring", status: "VALIDATED" as const, icon: ShieldCheck, body: "Core release `3575ffa` adds a composition factory that accepts only preconstructed context, channel, and wait ports, an explicit enablement flag, and a positive bounded poll count. It refuses disabled or invalid construction before a port is touched and never reads environment, resolves a secret, mounts a channel, or constructs filesystem, network, token, provider, database, or fixture adapters. Local composition tests use the in-memory channel only. Local `pnpm check` passed formatting, lint, strict TypeScript, 72 TypeScript tests with 18 integration tests skipped, and nine Python tests; Core Quality Gates `32656626047` and protected Azure deployment `32656626055` succeeded. No Core runner, Agent image, target profile/render, fixture, token, route, workspace, training, submission, or aggregation exists." },
  { date: "23 AUG 2026", title: "Topology correction makes both private exchange volumes explicitly ephemeral", status: "VALIDATED" as const, icon: ShieldCheck, body: "Review found that the initial named handoff/result volumes carried default local-driver persistence semantics despite the template’s one-shot intent. Agent release `52f3130` corrects both directional channel volumes to bounded 64k tmpfs mounts with restrictive mode. A source-level check confirmed two tmpfs declarations, two tmpfs devices, the directional read-only mounts, and no port or host bind. Local CI again passed formatting, strict TypeScript, 41 TypeScript tests, and four Python tests; Hospital Node Quality Gates `32621636293` succeeded. This does not create a target composite: Azure Compose rendering remains required and the proof remains blocked before fixture, token, lease, intent, stream, or workspace activity." },
  { date: "23 AUG 2026", title: "Core local channel fake closes one deterministic handoff/result exchange", status: "VALIDATED" as const, icon: ShieldCheck, body: "Core release `ca43355` adds an in-memory implementation of the private channel port for local coordinator tests only. It accepts one typed handoff and one opaque test result, rejects non-empty or duplicate delivery, and clears both states at closure without exposing a path, directory, socket, secret, provider, or runtime capability. The coordinator test now demonstrates one result delivery, one Core context closure, and empty terminal channel state. Local `pnpm check` passed formatting, lint, strict TypeScript, 70 TypeScript tests with 18 integration tests skipped under the non-integration command, and nine Python tests; Core Quality Gates `32619421561` and protected Azure deployment `32619421515` succeeded. No concrete shared channel, coordinator runner, Agent image, composite render, fixture, token, route, workspace, training, submission, or aggregation exists." },
  { date: "23 AUG 2026", title: "Private Agent file channel passes local and remote quality without target composition", status: "VALIDATED" as const, icon: ShieldCheck, body: "Agent release `d8857d3` adds a fake-tested private file-channel adapter that reads one opaque handoff and writes one exclusive scalar result through injected functions. It revalidates result shape and refuses unknown fields or impossible terminal combinations before a write; its in-memory test confirms no assignment identifier or command digest is serialized. The release also normalizes a non-executing topology template with one directional handoff mount, one directional result mount, opt-in profile, read-only roots, tmpfs, opaque secret references, no ports, no host bind, and no restart/default activation. Local CI passed formatting, strict TypeScript, 41 TypeScript tests, and four Python tests; Hospital Node Quality Gates run `32619077821` succeeded. This is not a deployed channel or proof: no reviewed Agent image, Core-side runtime runner, Azure composite render, fixture, token, route, workspace, training, submission, or aggregation exists." },
  { date: "23 AUG 2026", title: "One-shot Agent proof remains safely blocked before runtime composition exists", status: "BLOCKED" as const, icon: Workflow, body: "The Agent handoff release `8e53c22` passed Hospital Node Quality Gates run `32617651527`; Core release `a64488d` passed Core Quality Gates run `32617558237` and protected Azure deployment run `32617559177`. Read-only Azure checks found public Core liveness/readiness at HTTP 200 and the deployed aggregation worker explicitly disabled. However, the target layout contains no Agent proof composition, directional private handoff/result channel, Agent image source, or Agent runtime profile; the sandbox also lacks Docker for local Compose rendering. The proof is therefore blocked before fixture creation, token acquisition, lease, intent, stream, workspace write, or Core route access. This is a pre-route safety block, not a failed proof and not permission to retry." },
  { date: "23 AUG 2026", title: "Core and Agent prove private handoff rules locally without a shared channel or runtime request", status: "VALIDATED" as const, icon: ShieldCheck, body: "Core commit `a64488d` adds strict pure handoff/result validators, rejecting stale, unknown, locator-like, uncorrelated, and impossible-success records before any coordinator exists; its local quality passed formatting, lint, strict TypeScript, 67 TypeScript tests with 18 integration tests intentionally skipped, and nine Python tests. Agent commit `8e53c22` adds matching narrow parser/result contracts plus fake-first lease → command binding → intent → stream → workspace orchestration, producing only a run-keyed scalar terminal result. Agent CI passed formatting, strict TypeScript, 37 TypeScript tests, and four Python tests. All capabilities were injected fakes with in-memory SQLite/workspace state; no shared channel, token, Core/Azure request, fixture transfer, provider, deployment, training, submission, aggregation, hospital data, or clinical workflow occurred." },
  { date: "23 AUG 2026", title: "Private proof handoff prevents Agent assignment discovery before one-shot runtime work", status: "PROVISIONAL" as const, icon: Workflow, body: "The proof cannot safely discover an assignment through the guarded API, so the new coordination contract fixes a private rendezvous: Core creates one fresh generated proof context and writes one opaque handoff with only run ID, assignment ID, immutable descriptor facts, and expiry; the Agent independently leases it with its separate identity, validates command equality, then uses the existing intent/stream boundary. The Agent returns an even narrower scalar result keyed only by the private run ID; Core validates it and deletes the ephemeral channel/context. Target composition is limited to directional private tmpfs channels with no host path, persistent volume, public port, queue, discovery route, provider field, token, header/body, fixture byte, data field, training, submission, or aggregation. This is design only; no coordination code or runtime activity exists." },
  { date: "23 AUG 2026", title: "Opt-in Agent proof profile validates protected references without invoking Core", status: "VALIDATED" as const, icon: ShieldCheck, body: "Hospital Node Agent commit `ea97b69` adds L4e2 preparation only: a no-port, no-default, no-restart Compose profile with opaque secret reference and restricted tmpfs workspace, plus a preflight command that returns only scalar-safe readiness/refusal. It validates proof enablement, normalized HTTPS Core origin, protected reference classes, and positive bounds while exposing no origin, secret reference, workspace path, token, route, header/body, or provider field. Local CI passed formatting, strict TypeScript, 35 TypeScript tests, and four Python tests. The sandbox lacks Docker, so only a non-executing source-level Compose declaration check ran; target-runtime rendering remains required. No deployment, profile run, token, Core/Azure request, fixture, filesystem action, training, submission, aggregation, hospital data, or clinical workflow occurred." },
  { date: "23 AUG 2026", title: "Protected Agent deployment and one-shot generated-fixture proof are fixed as a separate gate", status: "PROVISIONAL" as const, icon: Workflow, body: "The L4e dossier defines the next runtime boundary before wiring: a non-public, opt-in Agent runner with no default profile, restart, public port, host data mount, trainer, update, submission, or aggregation action. It keeps the separate hospital-node identity, Core-only delivery, private ephemeral workspace, one generated fixture context, one intent, one full-body stream, and aggregate-safe closure. It requires source/release gates, Core health, disabled worker, zero prior runner, rebuilt one-shot invocation, and a published post-route failure before any retry. The dossier authorizes no runtime adapter, protected deployment, Core/Azure call, token acquisition, model transfer, training, submission, aggregation, hospital data, or clinical use." },
  { date: "23 AUG 2026", title: "Injected concrete Agent adapters pass local negative tests without acquiring a token or opening a request", status: "VALIDATED" as const, icon: ShieldCheck, body: "Hospital Node Agent commit `ec1c98f` implements L4d local logic behind closed injected ports. The typed Core client accepts reviewed config, constructs only the guarded intent/stream interaction classes, contains its token inside the adapter, validates scalar receipt/full-body facts, and projects no URL, raw header/body, provider field, or token. The private workspace requires an injected root/temporary/promote/remove port, hashes and bounds generated chunks, cleans interruption, and compensates a failed persistence step while returning only receipt-scoped scalar capability. Local CI passed formatting, strict TypeScript, 33 TypeScript tests, and four Python tests. Tests used generated token/request/filesystem doubles only; no real Core/Azure request, workload token, socket, host filesystem, provider, deployment, training, submission, aggregation, hospital data, or clinical workflow occurred." },
  { date: "23 AUG 2026", title: "Non-runtime review artifacts prove concrete-adapter safeguards without constructing an adapter", status: "VALIDATED" as const, icon: ShieldCheck, body: "Hospital Node Agent commit `6cb6662` implements L4c1/L4c2 as deterministic review artifacts only. The transport artifact classifies generated configuration/response facts into safe config, fixed-operation, terminal, or retryable outcomes without constructing a URL/path, client, header map, token, request, socket, or provider integration. The workspace artifact classifies abstract root/ownership/permission/symlink conditions and pathless cleanup/compensation transitions without importing filesystem or environment APIs. Local CI passed formatting, strict TypeScript, 29 TypeScript tests, and four Python tests. No concrete adapter, environment read, workload token, socket, filesystem action, Core/Azure request, deployment, training, update, submission, aggregation, hospital data, or clinical workflow occurred." },
  { date: "23 AUG 2026", title: "Concrete Agent transport and private-workspace safeguards are reviewed before runtime code", status: "PROVISIONAL" as const, icon: Workflow, body: "The L4c review fixes the future concrete adapter’s constraints without writing or invoking it. The composition root alone will own a normalized fixed HTTPS Core origin, literal hospital-node audience, bounded time/size values, and a private workspace root. The transport may construct only the guarded descriptor-intent and full-body stream routes; it will own the runtime token internally, deny redirects/Range/partial or encoded bodies, validate only safe response facts before yielding bytes, and project no URL, raw header/body, token, provider detail, or path. The workspace review requires exclusive temporary write, bounded integrity checking, same-root promotion, persistence compensation, and cleanup-failure readiness block while returning only a receipt-scoped scalar capability. L4c authorizes no environment read, token, socket, filesystem action, Core/Azure request, provider contact, deployment, training, submission, aggregation, hospital data, or clinical use." },
  { date: "23 AUG 2026", title: "Reusable bounded federated-workflow skill packages the research ledger discipline", status: "VALIDATED" as const, icon: FileText, body: "A reusable skill now packages the method used across these increments: documentation-first boundary dossiers; chronological public decision/evidence records; scalar-safe contracts and additive persistence; fake-first verification; separate concrete-adapter review; quality/deployment gates; one-shot synthetic proofs; aggregate-safe closure; and autonomous advancement without routine approval requests. The skill includes a redacted proof-record template and explicit stop conditions for secrets, locators, provider details, raw data, real clinical behavior, unbounded retries, public exposure, training, update submission, and aggregation. Its package validation passed after the required YAML validator dependency was installed. The skill is process guidance, not a new Core/Agent runtime capability and not evidence of deployment, transport, or scientific performance." },
  { date: "23 AUG 2026", title: "Agent fakes prove the typed Core and pathless workspace contract without a transport or filesystem", status: "VALIDATED" as const, icon: ShieldCheck, body: "Hospital Node Agent commit `afc98d1` implements L4b deterministic compatibility fakes only. The typed fake admits the descriptor-intent and full-body stream interaction classes, enforces intent-before-stream sequencing, returns scalar receipt/stream facts plus an async generated-byte iterator, and classifies invalid sequencing/idempotency and configured terminal/retryable outcomes without accepting a URL, token, header map, Range, provider field, or arbitrary route. The matching workspace fake keeps generated bytes in memory, exposes only aggregate lifecycle counts, permits exact promotion, and requires cleanup after mismatch/interruption. Local CI passed formatting, strict TypeScript, 25 TypeScript tests, and four Python tests. No Core call, workload token, socket, filesystem, provider, Azure resource, training, update, submission, aggregation, hospital data, or clinical workflow occurred." },
  { date: "23 AUG 2026", title: "Typed Core-client and private workspace boundary is fixed before an Agent may open a transport", status: "PROVISIONAL" as const, icon: Workflow, body: "The L4 dossier limits a future Agent transport to the separate hospital-node token seam and exactly two guarded Core route families: descriptor-only intent issuance and full-body stream. It projects only allowlisted scalar outcomes, checksum/length/content facts, and a byte iterator; it denies redirects, Range/partial behavior, arbitrary routes, raw headers/bodies, URL/locator/provider fields, and retry after body opening. The companion workspace port is pathless: it may consume validated bytes behind a private adapter and return only receipt-scoped scalar capability, while roots/paths/bytes/keys/provider facts remain outside contracts, SQLite, events, status, logs, and exports. This is design evidence only. L4b may implement deterministic fakes but may not open a socket, token source, filesystem, provider client, Azure resource, trainer, update, submission, or aggregation worker." },
  { date: "23 AUG 2026", title: "Agent verifies generated in-memory fixture facts and cleans terminal failure paths without a Core connection", status: "VALIDATED" as const, icon: ShieldCheck, body: "Hospital Node Agent commit `ec217dd` implements L3 as a fake-first local verifier. It incrementally hashes generated in-memory byte chunks, compares checksum, byte size, and the allowlisted content type to the persisted receipt, and returns only a receipt ID, terminal result class, and equality booleans. The in-memory workspace exposes aggregate lifecycle counters only. Exact generated bytes are promoted before local verified persistence, then discarded if persistence cannot complete; mismatches are cleaned and rejected; interruptions are cleaned and aborted; a terminal replay opens no workspace. Local CI passed formatting, strict TypeScript, 21 TypeScript tests, and four Python tests. No Core endpoint, OIDC token, HTTP stream, filesystem, real storage, Azure resource, training, submission, aggregation, hospital data, or clinical workflow was used." },
  { date: "22 AUG 2026", title: "Agent persists scalar-safe base-model receipt evidence without opening a Core connection", status: "VALIDATED" as const, icon: Database, body: "Hospital Node Agent commit `a9561ff` implements L2 locally: a strict versioned base-model read-receipt value, scalar observed-materialization facts, a pure terminal receipt state matrix, and additive SQLite receipt/materialization/event records. It accepts only assignment/read-intent IDs, immutable digests, checksum, positive byte size, the exact generated-model content type, expiry, and issued state; unknown locator-, token-, URL-, provider-, header/body-, path-, byte-, data-, and free-text-shaped fields are rejected. Exact receipt issue replay is idempotent, observed exact/mismatched facts produce terminal safe outcomes, and restart tests preserve terminality. Local CI passed formatting, strict TypeScript, 17 TypeScript tests, and four Python tests. No Core route, token, HTTP body, local workspace, fixture byte, training, update, submission, aggregation, hospital data, or Azure Agent execution occurred." },
  { date: "22 AUG 2026", title: "Azure validates one Core-mediated generated-fixture stream and closes its terminal state", status: "VALIDATED" as const, icon: ShieldCheck, body: "The opt-in stream profile commit `707cf23` passed Core Quality Gates run `32567581955` in 2 minutes 1 second and protected Azure deployment run `32567581950` in 2 minutes 45 seconds. After renewed public/container health checks returned HTTP 200 and the deployed aggregation worker retained its explicit default-disabled marker, the profile was invoked once through the explicit Compose file with `run --build`. It reused the separate synthetic hospital-node workload mapping, created one tiny generated non-clinical fixture through private Core setup, obtained one descriptor-only intent, and called the guarded Core stream route once. The runner privately verified full-body integrity and safe binary response behavior without printing a locator, provider detail, token, secret, header dump, or byte. Aggregate closure evidence was one completed stream session, zero active stream sessions, one consumed intent, zero issued/streaming intents, zero active leases, one closure event, and zero runner containers; aggregate expired assignment/lease counts include earlier bounded fixtures. No Agent persistence, training, update, submission, aggregation, real hospital, clinical data, or direct provider access occurred." },
  { date: "22 AUG 2026", title: "Next design gate is Agent receipt verification and synthetic local model persistence", status: "PROVISIONAL" as const, icon: Workflow, body: "The new Agent dossier fixes the next boundary before code: a typed Core client may obtain a descriptor-only read intent and consume exactly one Core-mediated response; a local verifier must bind it to immutable command/descriptor facts, check full-body headers, hash/count the generated bytes while writing a private temporary sink, and persist only redacted receipt/materialization state after exact EOF equality. SQLite, logs, status, and exports must never contain a token, URL, object locator/version, provider response, raw header/body, workspace path, dataset fact, or byte payload. No Agent implementation, Azure Agent proof, Python training, update, submission, aggregation, or clinical feature is authorized by this design record." },
  { date: "22 AUG 2026", title: "Core deploys a generated-model stream boundary; validation profile remains separate and dormant", status: "VALIDATED" as const, icon: ShieldCheck, body: "Core commit `d3516e4` adds migration `0014`, private stream sessions/events, an internal exact-read storage operation, stateful stream authorization service, and the guarded full-body stream route. It binds workload, leased assignment, issued intent, command/descriptor digests, verified artifact metadata, checksum, byte size, and allowlisted content type before opening the private adapter. The node receives only a Core binary response with no-store/nosniff/attachment headers; Range is rejected and no object key/version, bucket, URL, credential, provider response, or payload logging path is exposed. Local full quality passed with 83 TypeScript and nine Python tests; Core Quality Gates passed in 1 minute 55 seconds and protected Azure deployment completed in 2 minutes 46 seconds. Azure liveness/readiness were HTTP 200 and aggregation stayed disabled. No stream route or storage read ran in Azure yet." },
  { date: "22 AUG 2026", title: "Next proof is fixed: one generated Core-mediated stream with private fixture setup and terminal cleanup", status: "PROVISIONAL" as const, icon: Workflow, body: "The separate opt-in validation profile will reuse the existing separate synthetic hospital-node workload mapping and private client secret reference. Core-private setup may write one tiny generated non-clinical fixture and create only fresh generated surrounding records; it will never print a locator, provider configuration, credential, token, header, or payload. The simulated node calls the guarded Core stream route once and privately compares byte count/checksum, then the runner must verify redaction, completed/consumed state, terminal assignment/lease closure, fixture cleanup, zero runners, health, and disabled worker. The profile requires its own CI/deployment and `run --build` invocation to avoid stale image reuse. No Agent change, training, update, submission, dispatch, aggregation, real hospital, or clinical feature is authorized." },
  { date: "22 AUG 2026", title: "Core-mediated generated-model streaming dossier is complete before implementation", status: "PROVISIONAL" as const, icon: Workflow, body: "The public dossier now specifies the next boundary from nontechnical purpose through technical requirements, additive stream-session/event data design, full-body pilot workflow, Core-mediated architecture, zero-trust policy, no-locator/no-credential API, safe headers, integrity/closure rules, negative tests, bounded Azure proof, and AI handoff. The node may authenticate only to Core; Core may open a verified generated fixture through its private adapter and stream bytes over that same connection. The first slice rejects HTTP Range, permits one bounded full-body session, and must never disclose a bucket, object key/version, provider endpoint/response, credential, presigned URL, local path, patient field, or payload in logs. RFC 9110 and NIST SP 800-207 anchor the HTTP and resource-level policy choices. No stream route, storage read, Agent download, training, update, submission, dispatch, or aggregation behavior has been implemented." },
  { date: "22 AUG 2026", title: "Azure proves one guarded descriptor-intent issuance and terminal expiry without storage access", status: "VALIDATED" as const, icon: ShieldCheck, body: "After the workload-reuse correction passed local quality, Core Quality Gates, and protected Azure deployment, the validation runner was explicitly rebuilt and run once. It reused the already verified separate synthetic hospital-node workload mapping, created fresh generated surrounding Core facts, acquired the separate private token, and called only the guarded base-model-read intent route once. The receipt matched its assignment/lease/descriptor/command digest contract and contained no object key/version, URL, credential, provider response, or byte payload. Aggregate closure evidence was one expired proof assignment, zero active leases, zero issued intents, one closure event, and zero runner instances; Azure liveness/readiness were HTTP 200 and the aggregation worker stayed disabled. Neither the pre-route duplicate-workload rollback nor the stale-image rollback invoked the route. No storage resolution, download, byte transfer, training, update, submission, dispatch, aggregation, hospital operation, or clinical data use occurred." },
  { date: "22 AUG 2026", title: "Next design gate: Core-mediated model-stream authorization without locator or credential disclosure", status: "PROVISIONAL" as const, icon: Workflow, body: "The next work is design only. A future node-only Core stream endpoint would authenticate the existing hospital-node principal and validate a live immutable read-intent before Core itself retrieves and streams a verified generated model fixture. The node would receive no object address, bucket, provider credential, or presigned URL. Before implementation, the ledger must fix range/size/checksum/content-type rules, intent consumption and revocation races, cancellation/partial transfer behavior, rate limits, binary-safe audit evidence, retention, and failure redaction. No Agent download, local training, update, submission, dispatch, aggregation, real hospital connection, or clinical feature is authorized yet." },
  { date: "22 AUG 2026", title: "Read-intent proof requires an explicit runner rebuild after Compose reuses a stale local image", status: "BLOCKED" as const, icon: FileWarning, body: "The workload-reuse correction passed local full quality, Core Quality Gates, and protected Azure deployment. The next profile invocation still repeated the pre-route duplicate-workload failure because `docker compose run` reused the prior local image tag instead of rebuilding the corrected runner source. Its transaction rolled back before any guarded request, intent write, closure, storage operation, model access, training, update, submission, dispatch, or aggregation. The next invocation is therefore explicitly constrained to `run --build` with the deployed Compose file and the existing protected file references; it remains one guarded request only and requires renewed release-health and disabled-worker checks. A post-rebuild runtime failure must be recorded before any further attempt." },
  { date: "22 AUG 2026", title: "Read-intent proof stops before the route when the separate synthetic workload mapping is already present", status: "BLOCKED" as const, icon: FileWarning, body: "The newly deployed one-shot profile reached only its generated-fixture setup and then rolled back before any guarded request: inserting a workload for the private hospital-node token subject hit the existing issuer/subject uniqueness constraint. This confirms the prior bounded lease proof already established the separate synthetic workload mapping. No read-intent, assignment/lease closure, storage operation, object resolution, download, training, update, submission, dispatch, aggregation, ML-worker flow, or clinical data action occurred. The correction is narrow and documented: look up and reuse the active mapped hospital-node workload and its Core organization binding, create only fresh generated surrounding facts, then make one guarded request. The corrected source must pass quality/deployment and renewed Azure health/disabled-worker checks before one validation run." },
  { date: "22 AUG 2026", title: "Next proof is fixed: issue and expire one descriptor-only read intent", status: "PROVISIONAL" as const, icon: Workflow, body: "The next documented validation is a new opt-in, one-shot Azure profile. It will reuse the already separate active hospital-node workload mapped to the private `hospital-node-synthetic` token subject, seed only fresh generated surrounding Core facts and a verified descriptor mapping, and call the guarded base-model-read intent route once. The runner must prove assignment/lease/descriptor/digest consistency and reject any serialized `objectKey`, object version, URL, credential, provider response, or byte field; it will then expire the synthetic intent, lease, and assignment and append one scalar-safe closure event. It has no public port, storage client/resolution, daemon behavior, human route, ML-worker identity/callback, download, training, update, submission, dispatch, or worker enablement. The corrected profile must deploy and pass health/default-disabled-worker checks before its single run." },
  { date: "22 AUG 2026", title: "Core deploys a guarded descriptor-only base-model-read intent without storage access", status: "VALIDATED" as const, icon: ShieldCheck, body: "Core commit `d9b55fc` adds migration `0013`, the `hospital-node-base-model-read-intent/v1` receipt, one active intent per lease, exact replay, atomic safe-event persistence, and one guarded node-only POST route. The verified workload ID comes only from HospitalNodeAuthGuard; the request provides only a UUID idempotency key. Core rechecks active hospital-node workload, leased assignment, active matching lease, open round, active immutable descriptor, descriptor mapping, command digest, and model/preprocessing/checksum/size equality. The receipt contains IDs, digests, checksum, size, state, and expiry only; it excludes object keys/versions, URLs, credentials, provider data, bytes, preprocessing payload, and patient fields. Local full quality passed with 77 TypeScript and nine Python tests; Core Quality Gates passed in 1 minute 54 seconds and protected Azure deployment completed in 2 minutes 48 seconds. Azure liveness/readiness were HTTP 200 and the worker remained disabled. No Azure request, storage resolution, download, training, update, submission, or aggregation ran." },
  { date: "22 AUG 2026", title: "Next increment may begin: guarded descriptor-only base-model-read intent", status: "PROVISIONAL" as const, icon: Workflow, body: "With the verified descriptor registry deployed, the next implementation may now add the already-documented `hospital-node-base-model-read-intent/v1` boundary. It must be a HospitalNodeAuthGuard-only, lease-bound, idempotent receipt whose facts come from the active immutable descriptor registry and canonical command. It may return IDs, digests, size, state, and expiry only. The route must reject every human, ML-worker, wrong-workload, stale/revoked/consumed lease, changed-command, inactive descriptor, or cross-round case. It must not discover or serialize an object key, URL, credential, provider response, byte, model payload, preprocessing payload, or patient field; no storage adapter or download call is allowed. Its future Azure proof may issue then expire an intent only after dedicated migration, atomicity, redaction, guard, and health/disabled-worker tests pass." },
  { date: "22 AUG 2026", title: "Core deploys a verified base-model descriptor registry without a read path", status: "VALIDATED" as const, icon: Database, body: "Core commit `9fce888` adds migration `0012`, the private `base_model_archive` category, immutable base-model descriptor registry, independent scalar-safe registry events, canonical descriptor digest, and private service/repository/domain layers. It requires a verified same-federation artifact and preserves only descriptor identity, version, artifact ID, model/preprocessing digests, checksum, size, state, and correlation; it never copies the artifact object key into the registry, receipt, or event. Exact replay is allowed, conflicting version/artifact/digest facts are denied, and no HTTP, OIDC, lease, read intent, capability, storage, transfer, download, training, update, submission, dispatch, or worker behavior is added. Local full quality passed with 68 TypeScript and nine Python tests; Core Quality Gates passed in 1 minute 44 seconds and protected Azure deployment completed in 2 minutes 45 seconds. Azure liveness/readiness were HTTP 200 and the aggregation worker remained disabled. No mapping runtime profile ran, so this is deployed persistence evidence only." },
  { date: "22 AUG 2026", title: "Next increment is fixed: descriptor-only base-model-read intent, not a download", status: "PROVISIONAL" as const, icon: Workflow, body: "The next documented gate is a narrowly bounded base-model-read intent. A future node-only endpoint may issue a Core-owned, idempotent descriptor-only receipt only when the verified hospital-node principal owns an active unexpired lease bound to the unchanged canonical command and a Core-verified base-model descriptor. It must persist one active intent per lease with safe expiry/event evidence and return no locator, URL, credential, provider response, byte, model payload, preprocessing value, or patient field. Before code, the ledger requires an authoritative base-model-version-to-verified-artifact mapping, state/receipt schema, guard and redaction tests, atomic replay tests, and a later issue-and-expire proof that performs no storage access. No download, transfer, training, update, submission, dispatch, or worker activation is authorized." },
  { date: "22 AUG 2026", title: "Azure proves private assignment creation through one generated-fixture composition", status: "VALIDATED" as const, icon: ShieldCheck, body: "Core commit `0b48aeb` adds a separate opt-in Compose profile and one-shot runner for the private assignment-creation service. Local full quality passed with 60 TypeScript and nine Python tests; Core Quality Gates passed in 1 minute 48 seconds and protected Azure deployment completed in 2 minutes 46 seconds. The runner then built and ran once after a pre-start Compose interpolation issue was resolved using only protected secret-file references; no secret was read or printed and the profile used no token, endpoint, Keycloak, lease, storage, worker, or dispatch dependency. Generated prerequisite control facts were seeded, `SyntheticHospitalNodeAssignmentCreationService` created one `assigned` record and canonical command/event, and exact replay returned the same descriptor-only receipt. The runner enforced absence of `objectKey` and `credential`, expired its fixture, and wrote one safe closure event. Aggregate closure evidence was one closure event, one expired assignment, zero active assigned/leased records, and zero runner instances. Azure liveness/readiness were HTTP 200; the aggregation worker stayed disabled. No model access, transfer, training, submission, clinical data, or hospital integration occurred." },
  { date: "22 AUG 2026", title: "Core deploys a private synthetic assignment authority without widening its API", status: "VALIDATED" as const, icon: ShieldCheck, body: "Core commit `a0f423b` implements the documented private assignment-creation slice as a framework-free application service, port, domain policy, and PostgreSQL adapter extension. It has no controller, route, OIDC principal, key, profile invocation, migration, capability, lease, dispatch, transfer, submission, or worker change. Existing workload/participant/round/protocol facts must be active, same-organization/federation, open, and immutable-command consistent. The Core calculates the canonical digest itself, atomically writes one `assigned` record, one command, and one scalar-safe `assignment_created` event, and allows only an exact replay. Local full quality passed with 60 TypeScript and nine Python tests; Core Quality Gates passed in 1 minute 41 seconds and the protected Azure deployment completed in 3 minutes 1 second. Azure liveness/readiness returned HTTP 200, and the aggregation worker remained default-disabled. No creation-only profile ran, so this validates the deployed code boundary only." },
  { date: "22 AUG 2026", title: "Azure validates one bounded hospital-node lease without widening the data boundary", status: "VALIDATED" as const, icon: ShieldCheck, body: "Core commit `7d9218c` passed Core Quality Gates in 1 minute 37 seconds and its protected Azure deployment in 3 minutes 45 seconds. It adds an opt-in validation profile, the separate private `hospital-node-synthetic` Keycloak client, an independent protected secret mount, and a one-shot generated-fixture runner. A private client-credentials check succeeded without disclosing the token or secret; the profile then ran once, created only generated workload/federation/participant/round/assignment facts and a canonical descriptor-only command, and called the guarded lease route once. The runner enforced assignment/digest equality and rejected a response containing `objectKey` or `credential`. Aggregate closure evidence was one expired synthetic assignment, zero active synthetic assignments, zero active synthetic leases, and one safe closure event; the runner was absent afterward. Azure liveness/readiness returned HTTP 200 and the aggregation-worker gate remained disabled. No human route, ML-worker identity/callback, object transfer, model training, update submission, real node, hospital data, or clinical feature was used." },
  { date: "22 AUG 2026", title: "Bounded node-proof design selects a separate private client and fixture", status: "VALIDATED" as const, icon: Workflow, body: "Before invoking the deployed lease route, the ledger fixed a narrow proof design: one new private `hospital-node-synthetic` Keycloak service client with its own Docker secret, `fedagg-hospital-node` audience, and `hospital_node` claim. It neither reuses the ML-worker client, secret, audience, callback path, nor workload mapping. The now-completed opt-in Core-network validation profile registered only generated federation/participant/round/assignment facts and a canonical descriptor-only command, requested one lease, and expired the fixture immediately. The proof establishes identity separation, principal binding, command retrieval, descriptor-only response enforcement, and bounded lease closure only; it does not establish object transfer, training, update submission, a real hospital connection, or clinical-data processing." },
  { date: "22 AUG 2026", title: "Core deploys its first hospital-node-only lease route", status: "VALIDATED" as const, icon: ShieldCheck, body: "Core commit `0fd4e4b` adds `POST /v1/workload-assignments/:assignmentId/lease`, guarded solely by the dedicated HospitalNodeAuthGuard. The controller derives the workload ID from the verified workload principal, requires a UUID idempotency key, and returns only a lease receipt and the already persisted descriptor-only `hospital-node-command/v1` payload. Its test confirms principal-bound service input and verifies the serialized response contains neither `objectKey` nor `credential`. Full local CI passed with 50 TypeScript and nine Python tests, including migration/integration execution; Core Quality Gates passed in 1 minute 38 seconds and the protected Azure deployment completed in 2 minutes 44 seconds. Public liveness and strict readiness returned HTTP 200. The route was initially dormant; it was later exercised only by the separate bounded synthetic identity and generated fixture described in the newer ledger entry. No assignment-creation authority, capability, artifact transfer, update/submission route, real node, hospital data, or worker-gate change was introduced by this route slice." },
  { date: "22 AUG 2026", title: "Command-route prerequisite fixed: Core must own the canonical node command", status: "PROVISIONAL" as const, icon: Workflow, body: "Review of the implemented Core state found that its protocol-version record stores algorithm, architecture, and a configuration digest, but not every field required by the Agent’s already-tested `hospital-node-command/v1` validator. A digest cannot safely reconstruct proximal coefficient, local epochs, model/preprocessing digests, or base-model checksum/size. Before any lease controller is added, the Core will therefore store one canonical descriptor-only command payload alongside its computed digest at assignment creation. The payload has only immutable identifiers, expiry, safe algorithm/training parameters, digests, and size/checksum; it excludes provider URLs, object keys, credentials, bytes, paths, free text, and patient fields. This is a design-and-schema prerequisite, not a route, human command editor, node call, Keycloak client, capability, or deployment." },
  { date: "22 AUG 2026", title: "Core persists one safe, idempotent hospital-node lease per assignment", status: "VALIDATED" as const, icon: Database, body: "Core commit `263e596` adds the PostgreSQL hospital-node assignment repository and a migrated-database integration test. The adapter returns context only when assignment, workload, and participant records agree; returns the existing `(assignment_id, idempotency_key)` lease on recovery; refuses a second active lease; advances only an exact assigned workload/digest to `leased`; and appends one scalar-only `lease_issued` event. Migration `0010` is now registered in the migration journal, and a partial database unique index permits only one active lease for each assignment. Full local CI passed with 49 TypeScript and nine Python tests; Core Quality Gates passed in 1 minute 37 seconds and the protected Azure deployment completed in 2 minutes 49 seconds. Public liveness and strict readiness returned HTTP 200. No Core controller/route, Keycloak client, capability, command body, artifact update, submission, real node, hospital data, or worker-gate change exists yet." },
  { date: "22 AUG 2026", title: "Core separates the hospital-node OIDC audience from the ML worker", status: "VALIDATED" as const, icon: ShieldCheck, body: "Core commit `92c6c53` adds a dedicated `HospitalNodeAuthGuard` provider that verifies only the `fedagg-hospital-node` audience, requires an active local workload principal, and rejects every workload kind other than `hospital_node`. Its two deterministic tests prove an active hospital-node identity attaches to the workload request while an active `ml_worker` is denied. The existing internal worker-callback guard remains on its different `fedagg-worker-callback` audience and is unchanged; no controller uses the new guard yet. Full local CI passed with 48 TypeScript and nine Python tests, including migration/integration execution; Core Quality Gates passed in 1 minute 42 seconds and the protected Azure deployment completed successfully. Public liveness and strict readiness both returned HTTP 200. No Keycloak node client, API route, token, capability, Node request, hospital data, or worker-gate change was added; the worker remains explicitly disabled by default." },
  { date: "22 AUG 2026", title: "Core deploys additive hospital-node assignment, lease, and safe-event schema", status: "VALIDATED" as const, icon: Database, body: "Core commit `31e7588` adds the reviewed `0010_hospital_node_assignments.sql` migration and matching schema declarations for Core-owned assignment, lease, and append-only safe-event records. Foreign keys bind assignments to existing participant/round/workload records; unique constraints prevent duplicate round-workload assignments and duplicate assignment/idempotency leases. The only stored evidence is digest, deadline/state, correlation, safe event type, and scalar-safe details—never a data field, local path, token, capability, URL, object key, artifact byte, or remote response. Full local CI again passed with 46 TypeScript and nine Python tests, including migration/integration execution; Core Quality Gates passed in 1 minute 36 seconds and the protected Azure deployment completed successfully. Public liveness and strict readiness both returned HTTP 200. No persistence adapter, API controller, audience/guard, Keycloak client, capability, submission route, real node, hospital data, or worker-gate change exists yet." },
  { date: "22 AUG 2026", title: "Core adds the first isolated hospital-node lease policy slice", status: "VALIDATED" as const, icon: ShieldCheck, body: "Core commit `79bdcee` begins the documented hospital-node implementation in the narrowest possible layer: shared assignment/lease vocabulary, a domain transition matrix, and an application service that permits a lease only for an active `hospital_node` workload with an active participant, open round, assigned state, and future deadline. It includes three deterministic tests that reject both an `ml_worker` and an expired assignment before any repository write. There is still no Core migration, database adapter, Nest controller, OIDC audience/guard, Keycloak client, artifact capability, submission route, Node-to-Core request, or Azure node. The local full suite passed with 46 TypeScript and nine Python tests; Core Quality Gates passed in 1 minute 32 seconds and the protected Azure deployment completed successfully. Public liveness and strict readiness both returned HTTP 200. The default-disabled aggregation worker was not enabled or changed." },
  { date: "22 AUG 2026", title: "Core hospital-node workload contract is designed before integration", status: "PROVISIONAL" as const, icon: Workflow, body: "A new Core contract design now defines the required path for Hospital Node integration without shortcutting through an existing human or ML-worker endpoint. It requires a dedicated `fedagg-hospital-node` audience and guard; active `hospital_node` workload, organization, participant, round, deadline, and lease checks; additive assignment/lease/submission/event tables; descriptor-only artifact intent and verified submission; allowlisted incompatibility reporting; safe audit/outbox evidence; and a tightly bounded Azure synthetic proof. The design is explicit that the existing `hospital_node` workload kind and `model_update_archive` category are not by themselves a usable node API. No Core route, migration, Keycloak client, storage capability, human-route reuse, Azure node, hospital data path, or deployment has been added. The next implementation work begins in Core domain/schema/authorization tests, then contract-controlled HTTP routes." },
  { date: "22 AUG 2026", title: "Hospital Node Agent adds an opt-in localhost-only synthetic status read", status: "VALIDATED" as const, icon: GitCommitHorizontal, body: "Commit `e11d661` gives the Hospital Node Agent its first directly testable service boundary: an opt-in `GET /status` listener whose normal host is loopback only. A fixed redacted payload says only that the service is synthetic, Core integration is unconfigured, and local data is not exposed. The status process stays disabled unless explicitly enabled; binding to a private Compose network requires a distinct test-mode flag, and the supplied Compose profile maps its port only to the Docker host loopback. Hospital Node Quality Gates #5 passed remotely in 20 seconds, exercising the loopback response and the private-network refusal. Docker is not available in this sandbox, so the image and Compose profile are static test artifacts, not container runtime evidence. There is still no Core connectivity, public port, deployment, raw data path, model byte endpoint, hospital asset, or clinical feature." },
  { date: "22 AUG 2026", title: "Hospital Node adapters enforce scoped synthetic transfer boundaries", status: "VALIDATED" as const, icon: ShieldCheck, body: "The Hospital Node Agent has completed its next in-memory adapter slice across commits `deee8a6` and `1dcff80`. A scoped object capability now requires the exact assignment, operation, and unexpired lease; synthetic object storage rejects a checksum mismatch before returning only a checksum/size descriptor. The test token source is audience-bound, expiry-bound, and process-memory-only. A classifier distinguishes accepted, retryable temporary/timeout, and terminal remote outcomes without taking an automatic retry action. Hospital Node Quality Gates #3 and #4 both passed remotely; the final gate completed in 30 seconds with ten TypeScript tests and four Python FedAvg/FedProx tests. This is contract-bound synthetic evidence only: no Azure call, Core workload endpoint, human/ML-worker credential reuse, signed URL, object locator, persistent token, external storage, hospital data, or deployment has been introduced." },
  { date: "22 AUG 2026", title: "Hospital Node Agent receives restart-safe local control state", status: "VALIDATED" as const, icon: Database, body: "Commit `2468f4b` adds a deliberately narrow local control-state increment to the private Hospital Node Agent. The synthetic node now uses Node.js 22 built-in SQLite for a composite `(assignment_id, idempotency_key)` run record plus append-only event labels. Its application layer returns a recovered terminal run before it can train or submit again; a file-backed restart test proves that an already accepted run issues no duplicate fake-Core submission. The new in-process Core submission/assignment fakes remain contract-test adapters rather than a network client. Hospital Node Quality Gates #2 passed remotely in 18 seconds, including five TypeScript tests and the four Python FedAvg/FedProx tests. The node still has no local status service, artifact capability, workload token client, retry classifier, object transfer, Core hospital-node routes, real hospital data, or deployment; Node’s experimental SQLite runtime support and the redacted status projection remain documented decisions for subsequent slices." },
  { date: "22 AUG 2026", title: "Next system selected and documented: separate Hospital Node Agent", status: "VALIDATED" as const, icon: Workflow, body: "The completed Core ledger now hands off to a separate hospital-local training workload rather than expanding the Core into a patient or hospital-information system. The new private `hstu-research/federated-aggregator-hospital-node` repository began with a documentation-first dossier covering product/non-technical and technical requirements, local-only schema, normal and exception workflows, deployment architecture, engineering standards, proposed workload API, and an AI implementation handoff. Commit `cfcf1dc` passed Hospital Node Quality Gates #1: strict formatting/type checks, four TypeScript synthetic command/execution tests, and four Python FedAvg/FedProx primitive tests. The baseline accepts only generated values; it does not implement real Core workload routes, hospital data, patient upload, blockchain/IPFS, a browser portal, or clinical inference. The next slice is local SQLite restart safety and fake Core/storage ports before separate Core workload-contract work begins." },
  { date: "22 AUG 2026", title: "One bounded worker callback accepted; Redis remains direct after non-promotion evidence", status: "VALIDATED" as const, icon: GitCommitHorizontal, body: "The private Azure test path has now completed one deliberately bounded, synthetic FedProx aggregation proof. Commits `e92b527`, `bba2dab`, and `98e3989` passed Core Quality Gates runs 32–34 and protected Azure deployments 17–19. The profile-gated runner created a synthetic federation, sealed round, two tiny worker-local parameter archives, verified descriptors, and one queue job; its first enablement exposed two configuration gaps—missing private callback URL and a non-root secret-read permission—which were repaired without widening public access or exposing the client secret. The existing dispatched synthetic job then completed through the real outbox, BullMQ worker, Keycloak client-credentials token, and authenticated descriptor-only callback. PostgreSQL recorded `succeeded|candidate_ready|enqueued|accepted`; the worker gate was immediately restored to its default disabled state, and Azure internal/public liveness and readiness returned HTTP 200. The separate primary/two-replica/three-Sentinel harness observed primary loss and a final `subjectively_down` state but no promotion during its bounded window; the topology was torn down, and the Core still uses a direct Redis URL rather than claiming Sentinel-aware failover." },
  { date: "22 AUG 2026", title: "Federated research portfolio moved to hstu-research", status: "VALIDATED" as const, icon: GitCommitHorizontal, body: "Repository ownership is now centralized under `hstu-research`: the private Core, public documentation ledger, private administrator portal, and private thesis research rebuild were transferred with their existing GitHub history and default branches. Local Core, portal, thesis, and the documentation workspace’s named GitHub remote now target the new organization; current source references and runbooks no longer name the former owner. Core Quality Gates run 31 and protected Azure deployment run 16 both completed successfully from `hstu-research/federated-aggregator-core`, confirming that the repository-local SSH release path remained intact. Render’s Core API source now identifies `hstu-research/federated-aggregator-core`; the documentation static site rebuilt successfully from `hstu-research/federated-aggregator-docs` commit `3a8e65b`. Azure remains the authoritative environment, while Render remains a backup path. Historical ledger dates are retained, but active instructions and deployment links use the new organization." },
  { date: "22 AUG 2026", title: "Private Azure workload identity verified; aggregation remains explicitly disabled", status: "VALIDATED" as const, icon: GitCommitHorizontal, body: "Azure remains the authoritative test control-plane environment. Core commit `d35bebc` completed Core Quality Gates run 30 and protected Azure deployment run 15 after the release path self-provisioned its protected test client-secret file, synchronized its activation helper, and corrected worker/bootstrap image packaging and a PostgreSQL bootstrap query type conflict. The private Keycloak service is healthy; its one-shot bootstrap acquired a client-credentials token and recorded an active local `ml_worker` mapping without exposing the subject, token, secret, raw model bytes, descriptors, or patient data. The aggregation worker starts only after that bootstrap succeeds, then logs its disabled state because `AGGREGATION_WORKER_ENABLED=false` remains the default. Public Azure liveness and readiness both returned HTTP 200. A bounded synthetic queue-and-callback exercise is the next required proof before any explicit worker enablement." },
  { date: "22 AUG 2026", title: "Azure authority retained; Render remains liveness-only and Redis stays direct", status: "VALIDATED" as const, icon: Lightbulb, body: "The operational hierarchy is now explicit: Azure owns the Core execution and release-control path, while the Render free API and Cloudflare-managed backup components remain non-primary continuity paths. Render liveness is publicly available, but strict readiness remains HTTP 503 because the object-storage dependency is not accepted as ready; it must not be used for workload dispatch, authority, or release state. The Azure Redis Sentinel revalidation also remains a negative result: Sentinels detected primary loss but recurring tilt and no-good-slave outcomes prevented a repeatable promotion. The test Core therefore continues to use a direct Redis URL until an isolated resilience pass produces stable failover evidence." },
  { date: "21 AUG 2026", title: "GitHub CI/CD validates and deploys the public Core test endpoint", status: "VALIDATED" as const, icon: GitCommitHorizontal, body: "The Core now has a protected `azure-test` GitHub Actions environment with a dedicated revocable SSH deployment key and verified host-key pin. On every `main` push, the workflow runs the full `pnpm run ci` gate, packages committed source, deploys it over SSH, and invokes the VPS atomic release runner. Commit `25c7073` completed both Core Quality Gates and the HTTPS test deployment successfully. The active Azure release is `25c70733229299a0b56ccf59c40ea7d19c26073f`; public liveness returned `{status: ok, component: api}`. Caddy alone is public; API dependencies, database, Redis, MinIO, dispatch, and telemetry remain private. This proves a test release path only, not Python-worker, OIDC, real storage, Sentinel, hospital, or clinical production readiness." },
  { date: "21 AUG 2026", title: "Public HTTPS Core test endpoint activated with private dependencies", status: "VALIDATED" as const, icon: GitCommitHorizontal, body: "Following the Azure resize to two vCPUs and roughly 3.8 GiB RAM, a test-only Core stack was activated. Caddy is the sole public service and obtained an ACME certificate for `https://nirog.koreacentral.cloudapp.azure.com`; the API, PostgreSQL, Redis, MinIO, telemetry collector, and dispatch publisher stay on the private Docker network. Internal liveness and readiness checks passed. The release automation now performs CI before SSH archive deployment and rolls the `current` release symlink back on failed readiness, but it cannot auto-run until the commits are pushed and the repository deployment environment receives a dedicated SSH key, host, user, and host-key values. This is an integration test endpoint with synthetic local storage/database credentials and placeholder OIDC—not a hospital or production service." },
  { date: "21 AUG 2026", title: "Core receives a private deployment definition and durable dispatch entry point", status: "VALIDATED" as const, icon: GitCommitHorizontal, body: "The Core now has a separately supervised, signal-safe dispatch publisher entry point and a private-by-default container definition for PostgreSQL, Redis, internal telemetry, migrations, the Node API, and dispatch worker. The compose definition binds no host ports and expects a reverse proxy to be the only future public HTTPS surface. Full CI still passes with 43 TypeScript and 4 Python tests, and the Node runtime image built successfully on Azure without any real secrets or Core containers starting. It is deliberately not a complete production stack: the selected 891 MiB VPS is below the capacity gate; real protected secrets, Azure NSG/DNS/TLS, owner recovery access, and a Python aggregation-worker runtime with queue consumer, workload identity, storage adapters, and callback path remain required before any Core deployment." },
  { date: "21 AUG 2026", title: "Azure runtime validation corrected Sentinel startup but did not prove failover", status: "PROVISIONAL" as const, icon: FileWarning, body: "A disposable Azure Docker run accepted the committed Collector configuration-validation command. The Redis Sentinel harness initially exposed a Docker-DNS startup race; it was corrected with a mounted startup script that waits for name resolution and monitors the resolved private test address. The corrected primary/two-replica/three-Sentinel topology started and reported its initial master without publishing host ports. During a controlled primary stop, Sentinel marked the primary down but repeatedly entered tilt on the 891 MiB runner and did not elect a replacement before teardown. This is incomplete failover evidence, not proof of Sentinel-aware Core behavior, Redis high availability, or production readiness. The current Core limiter still accepts one Redis URL." },
  { date: "21 AUG 2026", title: "Azure VPS hardened and Core source staged; capacity gate remains", status: "PROVISIONAL" as const, icon: Lightbulb, body: "The selected Ubuntu VPS now has verified key-based operator access, an active default-deny host firewall with rate-limited SSH only, Fail2ban, unattended updates, Docker/Compose, bounded container log rotation, and 2 GiB swap. Core commit `630722b` is staged without sandbox credentials or Git history. The server currently has roughly 891 MiB RAM, which is not accepted for the full Core topology. No Core API, PostgreSQL, Redis/Sentinel, worker, telemetry collector, portal, clinical data path, or public HTTPS endpoint has been deployed; Azure NSG restriction, recoverable key-only owner access, appropriate capacity, production secrets, and runtime entry points remain gates." },
  { date: "21 AUG 2026", title: "Azure VPS hosting boundary prepared for the Core deployment", status: "PROVISIONAL" as const, icon: Lightbulb, body: "The next deployment phase now separates the public reverse-proxy surface from private Core dependencies: Core API, PostgreSQL, Redis/Sentinel, BullMQ, Python worker, and OpenTelemetry remain private; only HTTPS is public, with TCP 80 retained only when HTTP-01 certificate issuance is used. SSH is restricted to Azure Bastion, VPN, or a temporary approved CIDR. The Render documentation mirror stays separate. This is a staging/production-preparation topology, not a claim that the Core is deployed or a permission to co-locate hospital systems, patient data, clinical images, or arbitrary future applications on one public host." },
  { date: "21 AUG 2026", title: "Documentation mirror deployed on Render with verified SPA routing", status: "VALIDATED" as const, icon: GitCommitHorizontal, body: "A Render Static Site now serves the public documentation mirror at `https://federated-aggregator-docs.onrender.com`. Render built the public `main` branch at commit `a198ceb` using `pnpm install --frozen-lockfile && pnpm vercel-build` and published `./dist/public`. Both the root route and the direct `/research-log` route were opened successfully after a `/*` to `/index.html` rewrite was saved. The existing managed documentation deployment remains online. The reproducible configuration is committed locally, but its GitHub push is blocked because the sandbox GitHub credential is invalid; until that credential is restored, the remote `main` branch has not received this deployment-configuration commit." },
  { date: "21 AUG 2026", title: "Phase 14 staged milestone: reproducible infrastructure validation artifacts", status: "VALIDATED" as const, icon: GitCommitHorizontal, body: "The Core now includes a disposable Redis primary/two-replica/three-Sentinel topology, a Sentinel runbook for controlled failover observation, and a Collector configuration-validation command artifact. The runbook requires evidence to be stored as a bounded reference/digest and explicitly flags whether the current single-URL limiter is Sentinel-aware rather than assuming it is. Static whitespace, format, lint, type, and test checks passed. Docker is unavailable in this sandbox, so the topology, failover, Collector validation, health, backend delivery, and alert receiver were not executed. These artifacts are a reproducible plan, not runtime assurance." },
  { date: "21 AUG 2026", title: "Phase 13 staged milestone: deployment evidence now has an expiry policy", status: "VALIDATED" as const, icon: GitCommitHorizontal, body: "Every new deployment attestation now carries a `reviewedAt` timestamp and a fixed 90-day `expiresAt`; the additive migration backfills existing records from their prior review time. The protected Core summary and portal-safe type include expiry only, never the underlying provider configuration, endpoint, credential, evidence reference, or pager destination. Expiry means a review is no longer current enough for this product policy; it does not diagnose a deployment fault or prove configuration drift. Full CI passed 43 TypeScript tests plus 4 Python tests and PostgreSQL migration/integration execution; the portal build passed. A real multi-instance Redis outage/failover exercise and deployed Collector/backend/alert-receiver validation remain deferred." },
  { date: "21 AUG 2026", title: "Phase 12 staged milestone: provider verification records uncertainty without retry", status: "VALIDATED" as const, icon: GitCommitHorizontal, body: "An active platform administrator can now resolve a `recovery_pending_verification` disposal with a bounded provider observation: `object_absent`, `object_present`, or `provider_unavailable`. Each resolution appends immutable evidence containing an opaque external reference, digest, verified identity, and correlation ID. It never calls storage, replays the old deletion, reuses approval, or claims sanitization, backup/replica erasure, or compliance completion. The portal recognizes the redacted lifecycle state only; it receives no reference, digest, raw provider response, storage locator, version, or browser resolution control. Full CI passed 43 TypeScript tests plus 4 Python tests and the new PostgreSQL migration/integration suite passed; the portal production build passed." },
  { date: "21 AUG 2026", title: "Phase 11 staged milestone: deployment attestations preserve evidence boundaries", status: "VALIDATED" as const, icon: GitCommitHorizontal, body: "The Core now records reviewed deployment-attestation references for storage lifecycle, telemetry/alerting, and shared rate-limiter scopes. Only a platform administrator can record an attestation; federation-owner governance readers receive a redacted summary containing scope, assurance label, digest, and review time. Raw provider configuration, credentials, object locators, model/artifact bytes, and patient data are excluded. Audit/outbox evidence stores the safe digest only. The administrator portal renders a protected deployment-evidence count and has no browser recording control. Full CI passed 42 TypeScript tests plus 4 Python tests, including PostgreSQL migration/integration execution; the portal production build passed. Attestation expiry, provider-backed recovery resolution, and live Redis/Collector topology tests remain deferred." },
  { date: "21 AUG 2026", title: "Phase 10 staged milestone: stale disposal recovery records uncertainty", status: "VALIDATED" as const, icon: GitCommitHorizontal, body: "A disposal stuck in `executing` for more than fifteen minutes can now be moved only by an active platform administrator, with a non-empty reason, into `recovery_pending_verification`. This appends immutable recovery, audit, and outbox evidence and deliberately makes no storage call or deletion claim. The Core rejects recovery of a fresh, non-executing, or unavailable disposal; it cannot silently reuse the prior execution path. The portal recognizes the redacted recovery state but exposes no browser recovery/execution control. Full CI passed 40 TypeScript tests plus 4 Python tests and the PostgreSQL migration passed. Provider-backed storage verification, Object Lock/lifecycle attestations, Redis failure-mode testing, and Collector runtime validation remain deferred." },
  { date: "21 AUG 2026", title: "Phase 9 staged milestone: shared controls and safe disposal lifecycle view", status: "VALIDATED" as const, icon: GitCommitHorizontal, body: "The Core now selects a Redis-backed atomic fixed-window limiter for privileged commands whenever `REDIS_URL` is configured; a no-Redis process-local limiter remains only a development fallback. A new federation-owner-authorized disposal summary exposes lifecycle state, approval count, correlation ID, timestamp, and bounded outcome category—but never object keys or versions, reasons, descriptors, raw audit/outbox records, artifacts, or model bytes. The administrator portal renders that redacted view and requires an explicit reason to record an owner approval; browser execution remains unavailable. Collector self-metrics and portable alert-rule templates were added. Full Core CI passed 39 TypeScript tests plus 4 Python tests and the portal build passed. Docker/Collector, Redis-failure, storage policy, and alert-receiver runtime validation remain deferred." },
  { date: "21 AUG 2026", title: "Phase 8 core delivered: bounded telemetry, operational throttling, and dual-authorized disposal", status: "VALIDATED" as const, icon: GitCommitHorizontal, body: "The Core can now emit best-effort, allowlisted OpenTelemetry metrics to a configured local OTLP Collector while preserving a no-op default and never letting telemetry failure affect governance state. Privileged retry, dead-letter, disposal-request, approval, and execution commands are rate-limited per verified local principal and operation class before mutation, returning explicit 429/Retry-After information when blocked. A reviewed candidate can enter a storage-disposal request only after a `dispose_authorized` review; the requester cannot approve it, two distinct active federation-owner governors must approve, and an active platform administrator executes a frozen version-aware storage locator. Request, approval, execution result, correlation, and bounded failure category are immutable; candidate/release/audit lineage is retained. The full CI gate passed 39 TypeScript tests plus 4 Python tests. The portal remains review-only because no safe disposal-summary read model yet exists." },
  { date: "21 AUG 2026", title: "Phase 7 delivered: separate human administrator portal and safe governance read models", status: "VALIDATED" as const, icon: GitCommitHorizontal, body: "The private `hstu-research/federated-aggregator-admin-portal` repository contains a separate React/Vite human control surface for delivery operations, candidate/release lineage, and review-only retention work. The Core exposes only federation-scoped, role-authorized candidate/release summaries; it never returns artifact/model bytes, patient data, raw event payloads, approval-policy internals, or release envelopes through the portal. Retry/dead-letter controls require explicit reasoned confirmation and preserve correlation/audit evidence. The portal uses visibly labeled preview fixtures until a Core URL, explicit federation ID, and future OIDC session adapter are configured. The Core CI passed 36 TypeScript tests plus 4 Python tests, including protected PostgreSQL read-model integration coverage; the portal production build passed." },
  { date: "21 AUG 2026", title: "Phase 6 core delivered: dependency readiness and controlled outbox operations", status: "VALIDATED" as const, icon: GitCommitHorizontal, body: "The control plane now distinguishes process liveness from dependency-aware readiness across PostgreSQL, Redis, and object storage. Transactional-outbox events carry explicit pending, published, or dead-lettered delivery state, safe failure metadata, and immutable operator-action evidence. Only an active platform administrator can inspect delivery summaries, request a retry, dead-letter an unpublished event, or review retention-due candidates; event payloads stay immutable and are not returned by the operations view. The publisher excludes dead-lettered events from claim, acknowledgement, and failure paths. Full CI passed 36 TypeScript tests plus 4 Python tests." },
  { date: "21 AUG 2026", title: "Phase 5 core delivered: governed candidate, evidence, release, rollback, and retention lifecycle", status: "VALIDATED" as const, icon: GitCommitHorizontal, body: "An accepted aggregation result is now only the start of a governed candidate lifecycle, not an automatic model release. The core freezes the candidate descriptor, accepted worker-result digest, protocol, release policy, and retention date; it accepts descriptor-only evaluation evidence, enforces that a creator cannot approve their own candidate, counts distinct owner-organization approvals, and publishes an immutable release envelope bound to candidate, evidence, and approval digests. Controlled rollback either selects a compatible published fallback or suspends the release with a reason. Retention decisions are recorded for review only; no object is automatically deleted. The full CI gate passed 32 TypeScript tests plus 4 Python tests." },
  { date: "21 AUG 2026", title: "Phase 4 core delivered: verified dispatch, deterministic aggregation, and safe worker reconciliation", status: "VALIDATED" as const, icon: GitCommitHorizontal, body: "A sealed round can now freeze its exact verified, round-bound model-update descriptors into a versioned Node–Python command with a canonical digest. A lease-aware transactional-outbox publisher dispatches only committed jobs through a retry-safe BullMQ job identity; the isolated Python worker performs deterministic weighted FedAvg/FedProx-compatible server aggregation and returns a descriptor-only result. A separate active ML-worker identity must match the federation-owner organization, and its result must match the dispatched command, immutable input evidence, and digest before candidate-ready state is recorded. Stale, malformed, mismatched, or duplicate callbacks cannot mutate candidate state. The full CI gate passed 31 TypeScript tests plus 4 Python tests, including PostgreSQL migration and reconciliation coverage." },
  { date: "21 AUG 2026", title: "Phase 3 core delivered: descriptor-only artifact intake and quarantine", status: "VALIDATED" as const, icon: GitCommitHorizontal, body: "The private core now issues a short-lived, checksum-constrained S3-compatible upload intent for a generated staging key while keeping artifact bytes outside the API and PostgreSQL. Artifact, upload-intent, and append-only verification records preserve only descriptors, manifests, integrity facts, audit events, and outbox evidence. Local membership and federation participation govern creation; metadata mismatch, expiry, or an unavailable object is quarantined. The PostgreSQL migration ran successfully and the Phase 3 suite passes 24 tests." },
  { date: "21 AUG 2026", title: "Phase 2 core delivered: persistence, local authorization, and controlled API edge", status: "VALIDATED" as const, icon: GitCommitHorizontal, body: "The private product-core repository now has a reviewed Drizzle/PostgreSQL migration covering governance, participation, protocol/round history, audit, idempotency, and transactional outbox records. A remote-JWKS OIDC adapter validates issuer/audience/signature; a verified human still requires an active local principal and membership scope. NestJS provides liveness, dependency-aware readiness, and an authorization-filtered federation read. The migration executed on PostgreSQL 16 and the Phase 2 evidence suite passes 17 tests, including atomic state/audit/outbox commit and rollback." },
  { date: "21 AUG 2026", title: "Product-core repository established: tested local governance baseline", status: "VALIDATED" as const, icon: GitCommitHorizontal, body: "The private `hstu-research/federated-aggregator-core` repository holds the Phase 0–1 foundation: strict TypeScript/Python workspaces, contracts and ports, local dependency topology, CI, and deterministic domain tests for scoped memberships, non-human workload separation, protocol immutability, and round transitions. The baseline retains the approved OIDC/Clerk-ready human boundary, local authorization authority, workload credential separation, and MetaMask deferral." },
  { date: "21 AUG 2026", title: "Handoff plan fixed: local authority required; Clerk supported; MetaMask deferred", status: "VALIDATED" as const, icon: GitCommitHorizontal, body: "The first core release requires local PostgreSQL records for users, organizations, memberships, workloads, federation scope, and evidence-backed authorization. Human sign-in remains provider-owned through OIDC; Clerk is an acceptable managed option for a later portal, but its session/organization context cannot replace backend policy. Hospital and worker identities remain separate short-lived credentials. MetaMask/SIWE is a future optional human authentication factor only, with no v1 schema or runtime path." },
  { date: "21 AUG 2026", title: "Engineering standard defined: modular monorepo, clean boundaries, and layered proof", status: "VALIDATED" as const, icon: GitCommitHorizontal, body: "The future core repository is specified as a pnpm/TypeScript workspace plus independent Python packages: NestJS API, Node dispatch worker, and private ML worker. Domain capabilities own policy; framework/storage/queue/identity concerns are adapters; Node–Python communication is versioned schema plus golden fixtures; clean-room behavior is preserved as verified reference tests rather than imported product coupling." },
  { date: "21 AUG 2026", title: "Full core wiring specified: authority, artifacts, jobs, workers, and recovery", status: "VALIDATED" as const, icon: GitCommitHorizontal, body: "The architecture now makes every integration boundary explicit: OIDC/workload identities, NestJS command authority, PostgreSQL state/audit/outbox, constrained object storage, Redis/BullMQ dispatch, a private Python/PyTorch worker, full callback reconciliation, network isolation, observability, and governed rollback. The core is intentionally a control plane, not a hospital-data repository." },
  { date: "21 AUG 2026", title: "Workflow boundary formalized: round-to-release evidence chain", status: "VALIDATED" as const, icon: GitCommitHorizontal, body: "The core workflow now separates protocol activation, eligible-node selection, local training, submission quarantine, frozen-input aggregation, evaluation evidence, accountable release approval, and rollback. It records FedProx μ as client-side provenance and excludes raw hospital data from the core." },
  { date: "20 AUG 2026", title: "Technical boundary accepted for review: Node.js control, Python ML", status: "PROVISIONAL" as const, icon: GitCommitHorizontal, body: "The next architecture gate proposes a NestJS modular control plane, PostgreSQL domain state, artifact storage, and a separate PyTorch worker contract. Earlier prototypes are explicitly classified as reuse, rewrite, or defer." },
  { date: "20 AUG 2026", title: "Requirements gate opened before core architecture", status: "PROVISIONAL" as const, icon: FileWarning, body: "The product now has a written non-technical analysis covering stakeholder outcomes, business value, governance, research gaps, risks, and explicit assumptions to accept before backend design." },
  { date: "20 AUG 2026", title: "One product first: aggregation core and admin portal", status: "VALIDATED" as const, icon: Lightbulb, body: "Hospital nodes, hospital portals, and external coordination layers are separated from the first product. The immediate concern is a trustworthy model-release control plane." },
  { date: "20 AUG 2026", title: "Terminology correction: FedProx does not change the server’s basic role", status: "VALIDATED" as const, icon: GitCommitHorizontal, body: "The proximal penalty is applied during local client optimization. The aggregator validates and combines declared client updates, records μ, and publishes the next compatible global model." },
  { date: "19 AUG 2026", title: "Legacy low-specificity / high-sensitivity result is retained as a diagnostic", status: "PROVISIONAL" as const, icon: FileWarning, body: "The pattern is not a thesis claim. It remains a debugging signal pending a corrected aggregation implementation, grouped data split, reproducible configuration, and independent metric recalculation." },
];

const chapterMarkers: Readonly<Record<number, { label: string; title: string; note: string }>> = {
  0: { label: "CURRENT RECORD", title: "Core-to-Agent handoff", note: "Source gates are green, but a deliberate pre-route block remains until the target private Agent proof composition exists." },
  16: { label: "STREAM RECORD", title: "Core-mediated delivery", note: "The deployment, proof contract, and deliberately limited generated-fixture transfer boundary." },
  19: { label: "INTENT RECORD", title: "Authorization before bytes", note: "Descriptor-only intent evidence, corrected pre-route failures, and explicit no-storage-access limits." },
  26: { label: "DESCRIPTOR RECORD", title: "Immutable model authority", note: "Registry, assignment, and lease decisions that freeze what later workload requests may reference." },
  34: { label: "AGENT RECORD", title: "Local synthetic boundary", note: "Private Agent state, restart safety, typed fakes, and the retained hospital-data prohibition." },
  45: { label: "OPERATIONAL RECORD", title: "Control-plane resilience", note: "Bounded worker evidence, hosting authority, and retained infrastructure uncertainty." },
  57: { label: "FOUNDATION RECORD", title: "Architecture decisions", note: "The research and authority assumptions that shaped the Core before runtime execution." }
};

const evidenceLanguage = {
  VALIDATED: "verified evidence",
  PROVISIONAL: "uncertainty retained",
  BLOCKED: "decision blocked"
} as const;

export default function ResearchLog() {
  return (
    <div className="doc-page research-page">
      <header className="doc-topbar log-topbar"><div className="log-institutional-seal" aria-label="Aggregator Ledger institutional archive seal"><span className="seal-rules" aria-hidden="true"><i /><i /><i /><b /></span><p>12 / RESEARCH LOG</p></div><span className="topbar-meta"><CalendarDays size={15} />GMT+6 chronology</span></header>
      <section className="page-title"><p className="folio">12.0 / DECISIONS OVER TIME</p><h1>Keep the failed runs.<br /><i>They are evidence too.</i></h1><p>Every consequential change records its context, evidence, decision, consequence, status, and next action. The log is chronological by design.</p></section>
      <section className="log-ledger">
        {entries.map((entry, index) => {
          const Icon = entry.icon;
          const chapter = chapterMarkers[index];
          return (
            <div className="ledger-record" key={entry.title}>
              {chapter ? <div className="log-chapter"><div className="log-chapter-tab"><span>{chapter.label}</span><small>VOLUME / 12</small></div><div><h2>{chapter.title}</h2><p>{chapter.note}</p></div><small>INDEX / {String(index + 1).padStart(2, "0")}—{String(Math.min(index + 9, entries.length)).padStart(2, "0")}</small></div> : null}
              <article className={`log-entry status-${entry.status.toLowerCase()}`} data-evidence-state={entry.status}>
                <div className="log-marker"><span>{String(index + 1).padStart(2, "0")}</span></div>
                <div className="log-date">{entry.date}</div>
                <div className="log-content"><div className="log-title-row"><h2>{entry.title}</h2><StatusStamp status={entry.status} /></div><p>{entry.body}</p><a href="#open-decisions">Evidence and decision record <ArrowUpRight size={15} /></a></div>
                <aside className="log-evidence"><div className="evidence-ledger-head"><span className="evidence-kicker">PROOF STATE</span><small>REC. {String(index + 1).padStart(2, "0")}</small></div><div className="evidence-state-pair"><Icon size={20} /><StatusStamp status={entry.status} /></div><strong>{entry.status}</strong><span>{evidenceLanguage[entry.status]}</span><small>chronology / 12</small></aside>
              </article>
            </div>
          );
        })}
      </section>
      <section id="open-decisions" className="open-questions"><div><span>OPEN DECISION / 008</span><h2>Which normalization policy belongs in the production vision model?</h2></div><p>Before the actual vision architecture enters a federated round, document whether BatchNorm buffers are aggregated, retained locally, frozen, or replaced. The answer must be tested on that architecture, not inferred from the earlier MLP simulation.</p><StatusStamp status="BLOCKED" /></section>
    </div>
  );
}
